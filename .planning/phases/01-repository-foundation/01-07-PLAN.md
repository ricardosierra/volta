---
phase: 01-repository-foundation
plan: 7
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - tools/ci/validate-repo.sh
  - tests/tools/run_negative_checks.sh
  - tests/tools/README.md
autonomous: true
requirements: [FND-06]
must_haves:
  truths:
    - "validate-repo.sh detecta as 8 violações documentadas em TESTS.md, provado por 8 casos plantados que fazem o script falhar de verdade"
    - "validate-repo.sh também detecta função acima de 50 linhas e dessincronia entre packages/shared/config e apps/mobile/resources/config (as 2 regras adicionais de REPO-009 além das 8 de TESTS.md)"
    - "As fixtures de teste negativo nunca ficam permanentemente no código escaneado — são injetadas, verificadas e removidas pelo mesmo script"
    - "Depois de rodar os 10 casos negativos, validate-repo.sh volta a passar limpo (prova de que a limpeza funcionou)"
  artifacts:
    - path: "tools/ci/validate-repo.sh"
      provides: "8 regras + 2 adicionais (função >50 linhas, sincronia de config), todas verificadas por máquina"
    - path: "tests/tools/run_negative_checks.sh"
      provides: "harness que injeta cada violação, roda validate-repo.sh, confirma falha, remove, confirma limpo no final"
  key_links:
    - from: "tests/tools/run_negative_checks.sh"
      to: "tools/ci/validate-repo.sh"
      via: "chamada direta + assert de exit code"
      pattern: "validate-repo\\.sh"
---

<objective>
Estender `tools/ci/validate-repo.sh` (REPO-009) com as 2 regras que ainda faltam (função acima
de 50 linhas; sincronia `packages/shared/config` ↔ `apps/mobile/resources/config`) e provar,
com casos plantados de verdade, que as 8 regras de `TESTS.md` — mais essas 2 — realmente fazem
o script falhar quando devem.

Purpose: `.gsd/phases/01-repository-foundation/TESTS.md` é categórico — "Um verificador que
não falha quando deve é pior que não existir — dá falsa segurança para 24 fases." Sem prova
negativa, as 10 regras são decorativas.

Output: `validate-repo.sh` com as 10 regras completas; `tests/tools/run_negative_checks.sh`
provando cada uma, com limpeza automática ao final.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CONTRIBUTING.md
@tools/ci/validate-repo.sh
@.gsd/phases/01-repository-foundation/TESTS.md
@.gsd/QUALITY_GATES.md

<interfaces>
`validate-repo.sh` já existe (criado no bootstrap GSD 00) e já implementa **6 das 8** regras
de `TESTS.md`: nomes proibidos (seção 3), TODO sem ref (seção 4), MOCK sem Replacement (seção
5), placeholder sem Replacement — mas SEM checar fase expirada (seção 6, incompleta), camadas
(seção 7), tamanho de arquivo — só arquivo, não função (seção 8, incompleta), links (seção 9).
Este plano:
1. Completa a seção 6 (placeholder com fase já passada).
2. Completa a seção 8 (função > 50 linhas).
3. Adiciona uma seção 10 nova (sincronia de config).
4. Prova as 8 regras de `TESTS.md` + essas 2 adicionais com casos negativos reais.

Leia `tools/ci/validate-repo.sh` por inteiro antes de editar — as edições abaixo são
**incrementais** sobre o arquivo existente, não uma reescrita.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Completar as 2 regras faltantes em validate-repo.sh</name>
  <files>tools/ci/validate-repo.sh</files>
  <read_first>
    - tools/ci/validate-repo.sh (arquivo inteiro — as seções 6 e 8 atuais, para editar incrementalmente)
    - .gsd/QUALITY_GATES.md (tabela "Registro" — de onde vem a lista de fases já fechadas)
  </read_first>
  <action>
    1. **Placeholder com fase de destino já passada** (completa a seção 6 existente). Depois
       do bloco atual que já checa `Replacement: GSD [0-9]{2}` ausente, adicione: extrair o
       número de fase de cada `Replacement: GSD (\d+)` encontrado, comparar contra as fases
       marcadas `✅ fechado` em `.gsd/QUALITY_GATES.md` (tabela "## Registro", linhas no
       formato `| NN | ✅ fechado | ... |`), e falhar se algum placeholder referenciar uma
       fase já fechada (o placeholder deveria ter sido substituído quando aquela fase
       terminou). Adicione ao script, dentro da seção 6 existente (depois do `bad_ph`
       atual):
    ```bash
    closed_phases=$(grep -E '^\| [0-9]{2} \| ✅' .gsd/QUALITY_GATES.md | sed -E 's/^\| ([0-9]{2}).*/\1/')
    expired_ph=""
    for gsd_num in $(echo "$ph" | grep -oE 'Replacement: GSD [0-9]{2}' | grep -oE '[0-9]{2}' | sort -u); do
      if echo "$closed_phases" | grep -qx "$gsd_num"; then
        expired_ph="$expired_ph$(echo "$ph" | grep "Replacement: GSD $gsd_num")\n"
      fi
    done
    if [ -n "$expired_ph" ]; then
      fail "PLACEHOLDER com fase de destino já fechada (deveria ter sido substituído):"
      printf "$expired_ph" | sed 's/^/    /'
    fi
    ```

    2. **Função acima de 50 linhas** (completa a seção 8 existente, que hoje só checa
       arquivo > 600 linhas). Adicione, depois do bloco `big` atual:
    ```bash
    big_func=$(find apps packages services -name '*.gd' 2>/dev/null | while read -r f; do
      awk -v file="$f" '
        /^func |^static func / { if (start) { len = NR - start; if (len > 50) print file":"start" ("len" linhas)" }; start = NR }
        END { if (start) { len = NR - start + 1; if (len > 50) print file":"start" ("len" linhas)" } }
      ' "$f"
    done)
    if [ -n "$big_func" ]; then fail "função acima de 50 linhas:"; echo "$big_func" | sed 's/^/    /'
    else ok "nenhuma função acima de 50 linhas"; fi
    ```
       (Heurística: distância entre declarações `func`/`static func` consecutivas no mesmo
       arquivo. Não é um parser real de GDScript — documente isso com um comentário acima do
       bloco: `# heurística: distância entre 'func' consecutivos; não lida com funções
       aninhadas ou lambdas multi-linha longas dentro de uma func curta.`)

    3. **Sincronia de config** (seção nova, 10, depois da seção 9 de links, antes do bloco
       final de resumo):
    ```bash
    echo "== 10. Sincronia packages/shared/config <-> apps/mobile/resources/config =="
    if [ -d packages/shared/config ] && [ -d apps/mobile/resources/config ]; then
      sync_diff=$(diff -rq packages/shared/config apps/mobile/resources/config 2>&1 || true)
      if [ -n "$sync_diff" ]; then
        fail "packages/shared/config e apps/mobile/resources/config divergem:"
        echo "$sync_diff" | sed 's/^/    /'
      else
        ok "config sincronizada"
      fi
    else
      note "pulado: um dos dois diretórios ainda não existe (ConfigService criado em GSD 01 / REPO-005)"
    fi
    ```

    4. Todas as `find`/`grep -r` das seções 3 a 8 (nomes proibidos, TODO, MOCK, placeholder,
       camadas, tamanho) devem **excluir** `tests/tools/fixtures` do escaneamento normal —
       essa pasta vai guardar fixtures **estáticas** de referência (não os testes injetados
       da Task 2, que são temporários). Adicione, logo depois de `SRC_GLOBS=(...)`:
    ```bash
    exclude_fixtures() { grep -v '/tests/tools/fixtures/'; }
    ```
       E encadeie `| exclude_fixtures` nas variáveis `banned`, `bad_todo`, `mock_files`,
       `ph`, `viol`, `big`, `big_func` onde fizer sentido (depois do `grep`/`find`, antes de
       decidir falha). Isso é defensivo — nesta fase `tests/tools/fixtures/` não guarda
       nenhum arquivo estático ainda (a Task 2 injeta e remove em runtime), mas evita que uma
       fixture estática futura quebre o CI normal.
  </action>
  <verify>
    <automated>./tools/ci/validate-repo.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/ci/validate-repo.sh` contains `função acima de 50 linhas`
    - `tools/ci/validate-repo.sh` contains `Sincronia packages/shared/config`
    - `tools/ci/validate-repo.sh` contains `fase de destino já fechada`
    - `tools/ci/validate-repo.sh` contains `exclude_fixtures`
    - `./tools/ci/validate-repo.sh` exits 0 on the current clean repo state
  </acceptance_criteria>
  <done>validate-repo.sh cobre as 10 regras (8 de TESTS.md + função>50 + sync de config) e continua passando limpo no estado atual do repositório.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Provar as 10 regras com casos negativos injetados e removidos</name>
  <files>tests/tools/run_negative_checks.sh, tests/tools/README.md</files>
  <read_first>
    - tools/ci/validate-repo.sh (versão já editada pela Task 1 — confirme os textos exatos de FALHA usados em cada seção antes de escrever os asserts)
    - .gsd/phases/01-repository-foundation/TESTS.md (lista canônica das 8 violações)
  </read_first>
  <action>
    Crie `tests/tools/run_negative_checks.sh`. Padrão: para cada caso, (a) injeta um arquivo
    de violação real dentro de um diretório já escaneado por `SRC_GLOBS`, prefixado
    `__negtest_` para fácil identificação e limpeza; (b) roda `./tools/ci/validate-repo.sh`;
    (c) assevera exit code 1 E que a mensagem de FALHA esperada apareceu na saída; (d) remove
    a injeção via `trap` (mesmo se o script for interrompido no meio); (e) ao final, roda
    `validate-repo.sh` mais uma vez e assevera exit 0 (prova de que a limpeza foi completa).

    ```bash
    #!/usr/bin/env bash
    # Prova que validate-repo.sh detecta de verdade as 10 violações que promete.
    # Cada caso injeta uma violação real, confirma a falha, remove, e no final confirma
    # que o repositório volta a passar limpo.
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1

    FAIL=0
    INJECTED=()

    cleanup() {
      for f in "${INJECTED[@]}"; do rm -f "$f"; done
      INJECTED=()
    }
    trap cleanup EXIT

    check_case() {
      local desc="$1" file="$2" content="$3" expect_grep="$4"
      mkdir -p "$(dirname "$file")"
      printf '%s\n' "$content" > "$file"
      INJECTED+=("$file")
      local out
      out=$(./tools/ci/validate-repo.sh 2>&1)
      local code=$?
      rm -f "$file"
      INJECTED=("${INJECTED[@]/$file}")
      if [ "$code" -eq 0 ]; then
        echo "FALHA NO TESTE NEGATIVO: '$desc' não fez validate-repo.sh falhar (deveria)."
        FAIL=1
        return
      fi
      if ! echo "$out" | grep -q "$expect_grep"; then
        echo "FALHA NO TESTE NEGATIVO: '$desc' falhou, mas sem a mensagem esperada ('$expect_grep')."
        FAIL=1
        return
      fi
      echo "OK (negativo provado): $desc"
    }

    check_case "1. arquivo chamado utils.gd" \
      "apps/mobile/src/core/__negtest_dir/utils.gd" \
      "class_name NegtestUtils
    extends RefCounted" \
      "arquivo-depósito proibido"

    check_case "2. TODO sem (GSD-XX/TASK-YYY)" \
      "apps/mobile/src/core/__negtest_todo.gd" \
      "# TODO: consertar isso um dia
    class_name NegtestTodo
    extends RefCounted" \
      "TODO sem"

    check_case "3. bloco MOCK sem Replacement Phase" \
      "apps/mobile/src/core/__negtest_mock.gd" \
      "## MOCK
    ## Replacement Task: TASK-999
    class_name NegtestMock
    extends RefCounted" \
      "MOCK sem Replacement"

    check_case "4. PLACEHOLDER-ART-999 sem Replacement" \
      "apps/mobile/src/core/__negtest_placeholder.gd" \
      "# PLACEHOLDER-ART-999
    class_name NegtestPlaceholder
    extends RefCounted" \
      "PLACEHOLDER sem"

    check_case "5. PLACEHOLDER com fase de destino já fechada" \
      "apps/mobile/src/core/__negtest_expired.gd" \
      "# PLACEHOLDER-ART-050
    # Replacement: GSD 00
    class_name NegtestExpired
    extends RefCounted" \
      "fase de destino já fechada"

    check_case "6. import de presentation/ dentro de territory/" \
      "apps/mobile/src/territory/__negtest_layer.gd" \
      "class_name NegtestLayer
    extends RefCounted
    const BAD := preload(\"res://src/presentation/camera/camera_rig.gd\")" \
      "camadas"

    check_case "7. arquivo com 700 linhas" \
      "apps/mobile/src/core/__negtest_biggo.gd" \
      "$(printf 'class_name NegtestBiggo\nextends RefCounted\n'; for i in $(seq 1 700); do echo "# line $i"; done)" \
      "acima de 600 linhas"

    check_case "8. link quebrado em docs/" \
      "docs/__negtest_broken_link.md" \
      "# Teste

    [quebrado](./este-arquivo-nao-existe.md)" \
      "links quebrados"

    check_case "9 (extra). função acima de 50 linhas" \
      "apps/mobile/src/core/__negtest_bigfunc.gd" \
      "$(printf 'class_name NegtestBigfunc\nextends RefCounted\nfunc _big() -> void:\n'; for i in $(seq 1 60); do echo "    pass # $i"; done)" \
      "função acima de 50 linhas"

    check_case "10 (extra). dessincronia packages/shared/config" \
      "packages/shared/config/__negtest_desync.tres" \
      "[gd_resource type=\"Resource\"]" \
      "divergem"

    echo ""
    if [ $FAIL -eq 0 ]; then
      echo "✅ os 10 casos negativos provaram que validate-repo.sh falha quando deve."
    else
      echo "❌ pelo menos um caso negativo não provou a falha esperada — validate-repo.sh tem um buraco."
    fi

    final_out=$(./tools/ci/validate-repo.sh 2>&1)
    final_code=$?
    if [ $final_code -ne 0 ]; then
      echo "❌ validate-repo.sh não voltou a passar limpo depois da limpeza dos casos negativos:"
      echo "$final_out"
      FAIL=1
    else
      echo "✅ validate-repo.sh volta a passar limpo depois da limpeza."
    fi

    exit $FAIL
    ```
       Torne o script executável. Ajuste os textos de `expect_grep` para bater exatamente
       com as mensagens reais produzidas pela Task 1 (`grep -q` deve encontrar a
       substring — confirme rodando manualmente antes de considerar a tarefa pronta).

    2. Crie `tests/tools/README.md`:
    ```markdown
    # tests/tools/

    Prova, com casos negativos reais (injetados e removidos), que `tools/ci/validate-repo.sh`
    detecta de verdade as 10 regras que promete. Rode com:

    ```bash
    ./tests/tools/run_negative_checks.sh
    ```

    Um verificador que não falha quando deve é pior que não existir — ver
    `.gsd/phases/01-repository-foundation/TESTS.md`.

    `fixtures/` fica reservado para fixtures estáticas futuras (nenhuma nesta fase); é
    excluído do escaneamento normal de `validate-repo.sh` via `exclude_fixtures()`.
    ```
  </action>
  <verify>
    <automated>./tests/tools/run_negative_checks.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tools/run_negative_checks.sh` is executable
    - `tests/tools/run_negative_checks.sh` contains exactly 10 calls to `check_case`
    - Running `./tests/tools/run_negative_checks.sh` prints `✅ os 10 casos negativos provaram que validate-repo.sh falha quando deve.`
    - Running `./tests/tools/run_negative_checks.sh` prints `✅ validate-repo.sh volta a passar limpo depois da limpeza.` and exits 0
    - After running the script, `git status --porcelain` shows no `__negtest_` files left behind
    - `./tools/ci/validate-repo.sh` exits 0 (repo clean) immediately after the negative-check script finishes
  </acceptance_criteria>
  <done>Os 10 casos (8 canônicos de TESTS.md + 2 adicionais) provam falha real de validate-repo.sh, e o repositório volta a passar limpo depois — sem nenhum arquivo de teste deixado para trás.</done>
</task>

</tasks>

<verification>
`./tools/ci/validate-repo.sh` passa limpo no estado normal do repositório.
`./tests/tools/run_negative_checks.sh` prova as 10 regras e deixa o repositório limpo ao
final (`git status --porcelain` vazio quanto a arquivos `__negtest_`).
</verification>

<success_criteria>
ACCEPTANCE A01-09 satisfeito: "validate-repo.sh detecta TODAS as 8 violações que promete —
8 testes negativos" (aqui, 10, incluindo as 2 regras adicionais de REPO-009 que TESTS.md não
tinha listado individualmente mas que o próprio TASKS.md exige).
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-07-SUMMARY.md`.
</output>
