---
phase: 01-repository-foundation
plan: 5
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - apps/mobile/src/core/save/save_data.gd
  - apps/mobile/src/core/save/save_migration.gd
  - apps/mobile/src/core/save/save_service.gd
  - apps/mobile/src/core/save/file_save_service.gd
  - apps/mobile/tests/unit/test_save_service.gd
  - apps/mobile/tests/fixtures/save_migration_v1_fake.json
autonomous: true
requirements: [FND-03]
must_haves:
  truths:
    - "Um perfil salvo e recarregado é idêntico (round-trip) em todos os blocos"
    - "Uma escrita interrompida no meio nunca corrompe o save anterior"
    - "profile.json corrompido recupera de profile.json.bak sem perder dados nem apagar nada"
    - "backup também corrompido recria o perfil e preserva os dois arquivos suspeitos como .corrupt-<timestamp>, nunca apaga"
    - "uma cadeia de migração 1→2 funciona mesmo sem nenhuma migração real ainda existir em produção"
    - "um campo desconhecido no JSON sobrevive a um ciclo de save/load"
  artifacts:
    - path: "apps/mobile/src/core/save/save_service.gd"
      provides: "interface SaveService (load_profile/save_profile/mark_dirty/export_blob/import_blob)"
    - path: "apps/mobile/src/core/save/file_save_service.gd"
      provides: "implementação em disco com escrita atômica e recuperação de corrupção"
    - path: "apps/mobile/src/core/save/save_data.gd"
      provides: "estrutura de dados com round-trip e preservação de campos desconhecidos"
    - path: "apps/mobile/src/core/save/save_migration.gd"
      provides: "base de migração encadeada (from_version/to_version/migrate)"
  key_links:
    - from: "apps/mobile/src/core/save/file_save_service.gd"
      to: "apps/mobile/src/core/save/save_data.gd"
      via: "SaveData.from_dict / to_dict"
      pattern: "SaveData\\.(from_dict|to_dict)"
    - from: "apps/mobile/tests/unit/test_save_service.gd"
      to: "apps/mobile/src/core/save/file_save_service.gd"
      via: "instancia direta, sem passar pelo Bootstrap"
      pattern: "FileSaveService\\.new\\("
---

<objective>
Implementar `SaveService` (REPO-007) — persistência confiável desde o primeiro dia, resolvendo
diretamente o RISK-009 (perda de save) registrado em `.planning/STATE.md`.

Purpose: settings, perfil, progressão e inventário de anos de jogo dependem deste mecanismo
nunca perder dado silenciosamente. É o subsistema mais crítico desta fase depois do próprio
projeto Godot existir.

Output: `FileSaveService` com escrita atômica (tmp → flush → backup → rename), recuperação de
corrupção em 2 níveis, e mecanismo de migração encadeada testado com uma migração fake.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/architecture/save-system.md
@docs/decisions/ADR-0003-save-system.md
@.planning/STATE.md

<interfaces>
Este plano NÃO depende de `Bootstrap` estar wireado — mesmo raciocínio do Plano 01-04:
`FileSaveService` é testado instanciado diretamente. A integração no `Bootstrap` de produção
acontece no Plano 01-10.

Local dos testes GUT: `apps/mobile/tests/unit/` (não o `tests/unit/` da raiz do repo — ver
`tests/README.md`: "Os testes do cliente ficam em apps/mobile/tests/"). O fixture de migração
fica em `apps/mobile/tests/fixtures/` — deliberadamente **dentro** do projeto Godot (não em
`tests/fixtures/saves/` na raiz, que é reservado para fixtures de migrações **reais**, a
partir de GSD 10, cujo schema de fato mudou). Documente essa escolha no SUMMARY: é uma
simplificação deliberada porque esta migração é fake/mecanismo-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SaveData e SaveMigration — os contratos</name>
  <files>apps/mobile/src/core/save/save_data.gd, apps/mobile/src/core/save/save_migration.gd</files>
  <read_first>
    - docs/architecture/save-system.md §1 (os 9 blocos), §4 (versionamento/migração)
  </read_first>
  <behavior>
    - `SaveData.to_dict()` seguido de `SaveData.from_dict(dict)` seguido de `to_dict()` de novo
      produz um `Dictionary` idêntico ao primeiro (round-trip estável).
    - Uma chave desconhecida dentro de um bloco (ex.: `player["nickname_legacy"] = "x"`, não
      modelada por nenhum campo de `SaveData`) sobrevive ao ciclo `from_dict` → `to_dict`
      porque cada bloco guarda um `_unknown: Dictionary` com as chaves não reconhecidas e as
      remescla na saída.
    - `SaveMigration.migrate(data)` de uma subclasse concreta transforma o `Dictionary` e o
      `meta.schema_version` correspondente avança de `from_version()` para `to_version()`.
  </behavior>
  <action>
    1. `apps/mobile/src/core/save/save_data.gd`:
    ```gdscript
    class_name SaveData
    extends RefCounted

    const CURRENT_SCHEMA_VERSION: int = 1
    const BLOCKS: Array[String] = ["settings", "player", "progress", "stats", "records", "wallet", "inventory", "achievements", "challenges"]

    var meta: Dictionary = {"schema_version": CURRENT_SCHEMA_VERSION, "app_version": "0.1.0", "created_at": "", "updated_at": "", "device_id": ""}
    var settings: Dictionary = {}
    var player: Dictionary = {}
    var progress: Dictionary = {}
    var stats: Dictionary = {}
    var records: Dictionary = {}
    var wallet: Dictionary = {}
    var inventory: Dictionary = {}
    var achievements: Dictionary = {}
    var challenges: Dictionary = {}

    func to_dict() -> Dictionary:
        var out := {"meta": meta.duplicate(true)}
        for block in BLOCKS:
            out[block] = get(block).duplicate(true)
        return out

    static func from_dict(data: Dictionary) -> SaveData:
        var save := SaveData.new()
        save.meta = data.get("meta", save.meta)
        for block in BLOCKS:
            save.set(block, data.get(block, {}))
        return save
    ```
       Nota sobre "campo desconhecido preservado": como cada bloco já é um `Dictionary`
       genérico (não uma lista fixa de propriedades tipadas), qualquer chave extra dentro de
       um bloco (ex.: `player["campo_novo"]`) já sobrevive naturalmente ao round-trip — não
       precisa de um `_unknown` separado. Onde a preservação REALMENTE precisa de cuidado é
       em `meta`: se uma versão futura adicionar uma chave a `meta` que esta versão do app
       não conhece, `meta = data.get("meta", ...)` já preserva o dicionário inteiro como veio
       (não reconstrói campo a campo), então também está coberto. Documente esse raciocínio
       no SUMMARY em vez de adicionar complexidade desnecessária.

    2. `apps/mobile/src/core/save/save_migration.gd`:
    ```gdscript
    class_name SaveMigration
    extends RefCounted

    func from_version() -> int:
        push_error("SaveMigration.from_version() não implementado em %s" % [get_script()])
        return -1

    func to_version() -> int:
        push_error("SaveMigration.to_version() não implementado em %s" % [get_script()])
        return -1

    func migrate(data: Dictionary) -> Dictionary:
        push_error("SaveMigration.migrate() não implementado em %s" % [get_script()])
        return data
    ```

    3. `apps/mobile/tests/fixtures/save_migration_v1_fake.json` — fixture usado só pelo teste
       de migração da Task 2 (não é um save real de produção):
    ```json
    {
      "meta": {"schema_version": 1, "app_version": "0.0.9", "created_at": "2026-01-01T00:00:00Z", "updated_at": "2026-01-01T00:00:00Z", "device_id": "test-device"},
      "settings": {},
      "player": {"old_nickname_field": "Legacy"},
      "progress": {}, "stats": {}, "records": {}, "wallet": {}, "inventory": {}, "achievements": {}, "challenges": {}
    }
    ```
  </action>
  <verify>
    <automated>GODOT_BIN=$(command -v godot || echo "/Applications/Godot_CLI.app/Contents/MacOS/Godot"); "$GODOT_BIN" --headless --path apps/mobile --check-only; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/core/save/save_data.gd` contains `class_name SaveData`
    - `apps/mobile/src/core/save/save_data.gd` contains `func to_dict() -> Dictionary`
    - `apps/mobile/src/core/save/save_data.gd` contains `static func from_dict(data: Dictionary) -> SaveData`
    - `apps/mobile/src/core/save/save_migration.gd` contains `func migrate(data: Dictionary) -> Dictionary`
    - `apps/mobile/tests/fixtures/save_migration_v1_fake.json` is valid JSON (`python3 -m json.tool < apps/mobile/tests/fixtures/save_migration_v1_fake.json >/dev/null` exits 0, or `node -e "JSON.parse(require('fs').readFileSync(process.argv[1]))"`)
  </acceptance_criteria>
  <done>SaveData e SaveMigration compilam; fixture de migração fake criado; round-trip é estrutural (dicionários genéricos por bloco preservam chaves desconhecidas automaticamente).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FileSaveService — escrita atômica, recuperação, migração encadeada</name>
  <files>
    apps/mobile/src/core/save/save_service.gd, apps/mobile/src/core/save/file_save_service.gd,
    apps/mobile/tests/unit/test_save_service.gd
  </files>
  <read_first>
    - docs/architecture/save-system.md §3 (escrita atômica passo a passo), §5 (recuperação de corrupção)
    - apps/mobile/src/core/save/save_data.gd (contrato criado na Task 1)
  </read_first>
  <behavior>
    Os 6 testes exigidos por `.gsd/phases/01-repository-foundation/TESTS.md` /
    `docs/architecture/save-system.md` §8, todos em `test_save_service.gd`:
    1. `test_round_trip_all_blocks` — salva um `SaveData` com valor não-default em cada bloco, carrega de volta, compara igual.
    2. `test_interrupted_atomic_write_preserves_previous` — simula uma escrita interrompida (arquivo `.tmp` criado e preenchido, mas o `rename()` final NUNCA acontece); carrega o perfil; o `profile.json` original (não tocado) ainda é válido e é o que carrega.
    3. `test_corrupted_json_falls_back_to_backup` — escreve um `profile.json.bak` válido e um `profile.json` corrompido (JSON inválido); `load_profile()` retorna `SaveResult.RESTORED_FROM_BACKUP` e os dados batem com o backup.
    4. `test_corrupted_backup_recreates_without_deleting` — `profile.json` E `profile.json.bak` corrompidos; `load_profile()` retorna `SaveResult.RECREATED`; os dois arquivos originais NÃO são apagados, existem renomeados como `profile.json.corrupt-<timestamp>` e `profile.json.bak.corrupt-<timestamp>`.
    5. `test_fake_migration_v1_to_v2` — registra uma migração fake `from_version()=1, to_version()=2` que renomeia `player.old_nickname_field` para `player.nickname`; carrega o fixture `save_migration_v1_fake.json` (schema_version 1); depois de `load_profile()`, `meta.schema_version == 2` e `player.nickname == "Legacy"`.
    6. `test_unknown_field_preserved` — injeta uma chave extra num bloco (ex.: `player["custom_test_field"] = "kept"`) antes de salvar; recarrega; a chave ainda está lá.
  </behavior>
  <action>
    1. `apps/mobile/src/core/save/save_service.gd` — interface (base "virtual", como
       `LogSink`/`SaveMigration`):
    ```gdscript
    class_name SaveService
    extends RefCounted

    enum SaveResult { OK, RESTORED_FROM_BACKUP, RECREATED }

    func load_profile() -> SaveResult:
        push_error("SaveService.load_profile() não implementado")
        return SaveResult.RECREATED

    func save_profile(now: bool = false) -> void:
        push_error("SaveService.save_profile() não implementado")

    func mark_dirty(section: StringName) -> void:
        push_error("SaveService.mark_dirty() não implementado")

    func export_blob() -> PackedByteArray:
        return PackedByteArray()

    func import_blob(_blob: PackedByteArray) -> int:
        return SaveResult.RECREATED
    ```

    2. `apps/mobile/src/core/save/file_save_service.gd` — implementação concreta.
       Estrutura obrigatória (nomes de método podem variar internamente, mas o comportamento
       abaixo é o contrato):
       - Construtor recebe `save_dir: String = "user://save/"` (parametrizável para os
         testes usarem um diretório isolado, ex.: `"user://test_save/"`, evitando conflito
         entre testes e com um save real de desenvolvimento).
       - `data: SaveData` mantido em memória; `_dirty: Dictionary` (Set de seções pendentes).
       - `save_profile(now)`: serializa `data.to_dict()` com `JSON.stringify(..., "  ")`,
         escreve em `profile.json.tmp`, `flush()`, se `profile.json` existir e for JSON
         válido copia para `profile.json.bak`, depois `rename("profile.json.tmp", "profile.json")`
         via `DirAccess`. Limpa `_dirty` ao final.
       - `load_profile()`:
         a. Se `profile.json` não existe → cria `SaveData.new()` default, retorna `RECREATED`.
         b. Lê `profile.json`; `JSON.parse_string()`; se `null` (JSON inválido) → tenta `.bak`.
            b1. `.bak` válido → usa os dados do backup, salva de novo (restaura o principal),
                retorna `RESTORED_FROM_BACKUP`.
            b2. `.bak` também inválido/ausente → renomeia AMBOS para
                `<nome>.corrupt-<unix_timestamp>` (nunca apaga), cria `SaveData.new()`
                default, salva, retorna `RECREATED`.
         c. JSON válido → roda a cadeia de migrações (ver abaixo) → `SaveData.from_dict()` →
            retorna `OK` (ou `RESTORED_FROM_BACKUP`/`RECREATED` se a migração veio de um
            caminho de recuperação).
       - Cadeia de migração: `var migrations: Array[SaveMigration] = []` (injetável via
         `register_migration(m: SaveMigration) -> void`, usado pelos testes para registrar a
         migração fake). Em `load_profile()`, depois de obter o `Dictionary` bruto, repita:
         `while true: var v = data["meta"]["schema_version"]; var next = migrations.filter(func(m): return m.from_version() == v); if next.is_empty(): break; data = next[0].migrate(data); ` —
         nunca pula versão (usa sempre a migração cujo `from_version()` bate com a versão
         atual).
       - `mark_dirty(section)`: adiciona a `_dirty`; não escreve em disco imediatamente (a
         decisão de QUANDO chamar `save_profile(true)` — fim de partida, pause, timer de 60s —
         é do consumidor, não deste serviço; documente isso num comentário no topo do arquivo
         em vez de implementar um `Timer`/hook de `NOTIFICATION_APPLICATION_PAUSED` aqui,
         porque isso requer um `Node` vivo na árvore de cena, que só existe a partir do
         Plano 01-10).

    3. Escreva os 6 testes descritos em `<behavior>` em
       `apps/mobile/tests/unit/test_save_service.gd`. Use `before_each`/`after_each` do GUT
       para criar um diretório de save isolado por teste (ex.:
       `"user://test_save_%d/" % randi()`) e limpar arquivos ao final, para que os testes
       nunca leiam/escrevam o save real de desenvolvimento nem conflitem entre si.
  </action>
  <verify>
    <automated>./tools/ci/test-client.sh</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/core/save/file_save_service.gd` contains `class_name FileSaveService`
    - `apps/mobile/src/core/save/file_save_service.gd` contains `enum SaveResult` or references `SaveService.SaveResult`
    - `apps/mobile/src/core/save/file_save_service.gd` contains `register_migration`
    - `apps/mobile/src/core/save/file_save_service.gd` contains `.corrupt-`
    - `apps/mobile/tests/unit/test_save_service.gd` contains exactly 6 `func test_` definitions matching the six scenarios in `<behavior>`
    - `./tools/ci/test-client.sh` exits 0 with these 6 tests included
  </acceptance_criteria>
  <done>Os 6 testes de save-system.md §8 passam: round-trip, escrita interrompida, corrupção→backup, backup ruim→recriação preservando arquivos, migração fake 1→2, campo desconhecido preservado.</done>
</task>

</tasks>

<verification>
`./tools/ci/test-client.sh` inclui e passa os 6 testes de `test_save_service.gd`. Nenhum
arquivo de save real do jogador é tocado pelos testes (diretórios isolados por teste).
Nenhuma corrupção resulta em arquivo apagado — sempre renomeado.
</verification>

<success_criteria>
RISK-009 (perda de save) está mitigado desde a Fase 1, exatamente como
`docs/architecture/save-system.md` §8 e `.planning/STATE.md` prometem. `SaveService` está
pronto para ser consumido por `Bootstrap` (Plano 01-10) e, mais tarde, por `progression/` (GSD
10) e cloud save (GSD 16).
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-05-SUMMARY.md`.
</output>
