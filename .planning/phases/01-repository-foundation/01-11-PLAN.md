---
phase: 01-repository-foundation
plan: 11
type: execute
wave: 6
depends_on: ["01-10"]
files_modified:
  - docs/performance/device-results.md
  - .planning/STATE.md
autonomous: false
requirements: [FND-01]
must_haves:
  truths:
    - "O APK de debug foi instalado e aberto num Android real, não apenas exportado"
    - "Cold start, FPS e safe area foram observados por um humano em hardware real, não estimados"
    - "docs/performance/device-results.md tem a primeira linha real preenchida (não mais 'pendente')"
  artifacts:
    - path: "docs/performance/device-results.md"
      provides: "primeira medição real de dispositivo, preenchida"
  key_links:
    - from: "docs/performance/device-results.md"
      to: ".planning/STATE.md"
      via: "registro do gate de fase 1 fechado ou da pendência explícita (RISK F01-07)"
      pattern: "device-results"
---

<objective>
Confirmar em um Android real o que o Plano 01-10 automatizou (REPO-012, parte humana):
instalar o APK de debug, abrir, medir cold start, FPS e safe area — e preencher
`docs/performance/device-results.md` com números reais em vez de "pendente".

Purpose: ACCEPTANCE A01-12/A01-13/A01-14 e o Success Criterion 6 do ROADMAP exigem
especificamente um APK "instalando num Android real" — isso não pode ser fabricado nem
inferido de um build bem-sucedido no CI. `.planning/STATE.md` já registra o risco F01-07
("Não haver aparelho Android disponível") como algo a deixar explícito se acontecer.

Output: `docs/performance/device-results.md` preenchido com a primeira medição real; ou, se
não houver aparelho disponível no momento da execução, a pendência registrada explicitamente
em `.planning/STATE.md` (não silenciosamente ignorada).
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
@/Users/sierra/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@docs/mobile/device-matrix.md
@.gsd/phases/01-repository-foundation/RISKS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automatizar o que der com adb (instalar, lançar, tentar medir)</name>
  <files>docs/performance/device-results.md</files>
  <read_first>
    - dist/android/volta-debug.apk (produzido pelo Plano 01-10 — confirme que existe antes de tentar instalar)
  </read_first>
  <action>
    1. Rode `~/Library/Android/sdk/platform-tools/adb devices` (ou `adb devices` se estiver
       no PATH). Se **nenhum** dispositivo aparecer, registre isso claramente no SUMMARY desta
       tarefa (não é uma falha desta tarefa — é a razão de a Task 2 virar um checkpoint) e
       pule os passos 2-4 abaixo.
    2. Se houver um dispositivo: `adb install -r dist/android/volta-debug.apk`.
    3. Tente medir o cold start com o utilitário padrão do Android:
       `adb shell am start -W -S -n com.ricardosierra.volta/com.godot.game.GodotApp` (o nome
       exato da Activity pode variar conforme o template de export do Godot 4.3 — se este
       comando falhar por "Activity não encontrada", descubra o nome real com
       `adb shell dumpsys package com.ricardosierra.volta | grep -A1 MAIN` e repita). O campo
       `TotalTime` da saída é o cold start em ms — registre-o.
    4. Se a Activity não puder ser resolvida por nome, use
       `adb shell monkey -p com.ricardosierra.volta -c android.intent.category.LAUNCHER 1`
       para abrir o app (sem medição automática de tempo) e documente que o cold start
       precisará ser cronometrado manualmente no checkpoint da Task 2.
    5. Preencha em `docs/performance/device-results.md`, na linha "Phase 1", os campos que
       puderam ser automatizados (Data, aparelho via `adb shell getprop ro.product.model`,
       Android via `adb shell getprop ro.build.version.release`, cold start se medido). Deixe
       "_pendente_" nos campos que só um humano observando a tela pode confirmar (FPS
       percebido como estável, safe area, versão visível na tela).
  </action>
  <verify>
    <automated>test -f docs/performance/device-results.md && grep -c 'Phase 1' docs/performance/device-results.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY desta tarefa declara explicitamente se um dispositivo Android estava conectado ou não
    - Se conectado: `docs/performance/device-results.md` tem "Aparelho" e "Android (versão)" preenchidos com valores reais de `adb shell getprop`
    - Se não conectado: nenhum campo de `docs/performance/device-results.md` foi preenchido com um valor inventado
  </acceptance_criteria>
  <done>Tudo que adb permite automatizar foi tentado e registrado; o que sobrou (confirmação visual) fica explícito para a Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirmar em Android real — cold start, FPS, safe area</name>
  <what-built>
    Um projeto Godot 4.3 completo (Fase 1 inteira): projeto scaffolded, Build/Log/Save/Config/
    EventBus/ServiceRegistry/Bootstrap implementados e testados, `validate-repo.sh`/`lint.sh`/
    `test-client.sh` verdes, CI configurado, e um APK de debug com uma cena mínima que mostra
    nome do app, versão, FPS e tier de dispositivo detectado.
  </what-built>
  <action>
    Checkpoint de verificação humana. Task 1 já automatizou tudo que dá para automatizar sem
    olhos num aparelho real (instalação via adb, tentativa de medição de cold start). Este
    passo pede a confirmação visual que só um humano pode dar. Revise os passos de
    `<how-to-verify>` abaixo e responda conforme `<resume-signal>`.
  </action>
  <verify>
    <automated>test -f docs/performance/device-results.md && ! grep -q '| Phase 1.*_pendente_.*_pendente_.*_pendente_.*_pendente_.*_pendente_.*_pendente_' docs/performance/device-results.md; echo "checked=$?"</automated>
  </verify>
  <how-to-verify>
    1. Conecte um Android real (tier Mid recomendado — ver `docs/mobile/device-matrix.md`) via
       USB com depuração habilitada.
    2. Se a Task 1 não conseguiu instalar automaticamente (nenhum dispositivo estava
       conectado durante a execução), rode agora:
       `~/Library/Android/sdk/platform-tools/adb install -r dist/android/volta-debug.apk`
       (gere o APK primeiro com `./tools/ci/build_android.sh debug` se `dist/android/volta-debug.apk`
       não existir mais).
    3. Abra o app manualmente no aparelho. Confirme, olhando a tela:
       - O texto mostra "VOLTA" e a versão `0.1.0`.
       - O contador de FPS fica estável perto de 60 (sem soluços perceptíveis) por pelo menos
         10 segundos navegando/parado na tela.
       - Nenhum texto fica escondido atrás do notch/câmera (safe area).
       - A tela permanece em portrait mesmo girando o aparelho.
       - Cronometre o cold start (toque no ícone → app visível e interativo) com um cronômetro
         ou a contagem `TotalTime` da Task 1 — meta: menor que 1,5 s.
       - Coloque o app em background e volte; confirme que não crasha.
    4. Preencha (ou peça para eu preencher, colando os números que você observar) as linhas
       restantes de `docs/performance/device-results.md` (FPS, safe area, versão exibida,
       observações).
  </how-to-verify>
  <resume-signal>
    Responda "approved" se tudo confirmado, ou descreva o que não bateu (ex.: "cold start
    2,1s" ou "texto sob o notch") para eu registrar como pendência explícita em
    `.planning/STATE.md` em vez de fechar o gate silenciosamente.
  </resume-signal>
  <done>docs/performance/device-results.md preenchido com números reais de um Android físico, confirmado pelo usuário; ou pendência explícita registrada em .planning/STATE.md se nenhum aparelho estava disponível.</done>
</task>

</tasks>

<verification>
`docs/performance/device-results.md` não contém mais "_pendente_" na linha "Phase 1" — ou, se
o usuário não tinha um aparelho disponível no momento, `.planning/STATE.md` registra
explicitamente essa pendência (mesmo padrão do risco F01-07 em
`.gsd/phases/01-repository-foundation/RISKS.md`: "se faltar aparelho, a fase fecha com
pendência explícita em STATUS.md/STATE.md").
</verification>

<success_criteria>
ACCEPTANCE A01-12 (APK instala e roda num Android real), A01-13 (cold start < 1,5s) e A01-14
(60 FPS estáveis) verificados por um humano em hardware real — ou a lacuna documentada sem
ambiguidade, nunca presumida.
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-11-SUMMARY.md`.
</output>
