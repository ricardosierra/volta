---
phase: 01-repository-foundation
plan: 6
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-05"]
files_modified:
  - apps/mobile/src/core/event_bus.gd
  - apps/mobile/src/core/events/README.md
  - apps/mobile/tests/unit/test_event_bus.gd
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "Um evento global (ex.: config carregada) pode ser assinado e emitido sem string solta (nada de emit('nome_qualquer'))"
    - "Desinscrever para de entregar o evento"
    - "Em debug, emitir o mesmo evento acima de um limite por segundo é detectável (EventBus é para eventos raros, não por frame)"
  artifacts:
    - path: "apps/mobile/src/core/event_bus.gd"
      provides: "sinais tipados (config_loaded, config_load_failed, save_loaded, save_written) + contador de emissão em debug"
  key_links:
    - from: "apps/mobile/src/core/event_bus.gd"
      to: "apps/mobile/src/core/build.gd"
      via: "Build.is_debug()"
      pattern: "Build\\.is_debug\\(\\)"
---

<objective>
Implementar `EventBus` (REPO-008) — eventos globais de baixa frequência, com sinais tipados
declarados explicitamente e uma trava de debug que torna visível o abuso ("evento por frame")
antes que ele vire hábito em fases futuras.

Purpose: `docs/architecture/overview.md` §3 é explícito — EventBus é para "muitos ouvintes
desacoplados, baixa frequência". Sem uma trava agora, será tarde demais para corrigir depois
que `gameplay/`, `territory/` e `ai/` já dependerem de um EventBus abusado por frame.

Output: `EventBus` com 4 sinais tipados iniciais (ligados aos serviços já existentes: Config e
Save) e um limite de emissões por segundo verificável em debug.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/architecture/overview.md

<interfaces>
De `apps/mobile/src/core/build.gd` (Plano 01-01): `Build.is_debug() -> bool`.

Este plano cria `src/core/events/` como diretório-mirror do padrão de pastas de
`docs/architecture/overview.md` §8, mas **sem** classes de payload ainda: os 4 eventos desta
fase usam apenas tipos primitivos (`String`, `int`) nos sinais, então não precisam de uma
classe de payload dedicada. Eventos futuros com payload complexo (ex.: `MatchStartedEvent` em
GSD 06) ganham seu arquivo aqui quando existirem — não invente esses eventos agora.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventBus — sinais tipados + assinatura/desassinatura</name>
  <files>apps/mobile/src/core/event_bus.gd, apps/mobile/src/core/events/README.md, apps/mobile/tests/unit/test_event_bus.gd</files>
  <read_first>
    - docs/architecture/overview.md §3 (Chamada direta vs Signal vs EventBus — quando usar cada um)
  </read_first>
  <behavior>
    - `bus.config_loaded.connect(callback)` seguido de `bus.emit_config_loaded()` chama `callback`.
    - `bus.config_loaded.disconnect(callback)` seguido de `bus.emit_config_loaded()` NÃO chama `callback` de novo.
    - `bus.emit_save_loaded(SaveService.SaveResult.OK)` entrega o valor `0` (OK) ao assinante.
  </behavior>
  <action>
    1. `apps/mobile/src/core/event_bus.gd`:
    ```gdscript
    class_name EventBus
    extends Node

    ## Eventos globais de baixa frequência, desacoplados. PROIBIDO emitir por frame — ver
    ## get_recent_emission_count() e o teste de limite em test_event_bus.gd.
    signal config_loaded()
    signal config_load_failed(reason: String)
    signal save_loaded(result: int)
    signal save_written()

    const MAX_EMISSIONS_PER_SECOND: int = 5

    var _emission_timestamps: Dictionary = {}

    func emit_config_loaded() -> void:
        _track_emission("config_loaded")
        config_loaded.emit()

    func emit_config_load_failed(reason: String) -> void:
        _track_emission("config_load_failed")
        config_load_failed.emit(reason)

    func emit_save_loaded(result: int) -> void:
        _track_emission("save_loaded")
        save_loaded.emit(result)

    func emit_save_written() -> void:
        _track_emission("save_written")
        save_written.emit()

    func get_recent_emission_count(signal_name: String) -> int:
        return (_emission_timestamps.get(signal_name, []) as Array).size()

    func _track_emission(signal_name: String) -> void:
        if not Build.is_debug():
            return
        var now := Time.get_ticks_msec() / 1000.0
        var timestamps: Array = _emission_timestamps.get(signal_name, [])
        timestamps.append(now)
        timestamps = timestamps.filter(func(t: float) -> bool: return now - t <= 1.0)
        _emission_timestamps[signal_name] = timestamps
        if timestamps.size() > MAX_EMISSIONS_PER_SECOND:
            var msg := "EventBus: '%s' emitido %d vezes no último segundo (limite %d) — EventBus é para eventos raros, não por frame" % [signal_name, timestamps.size(), MAX_EMISSIONS_PER_SECOND]
            if Log:
                Log.warn(Log.Category.ERROR, "event_bus_rate_exceeded", {"signal": signal_name, "count": timestamps.size()})
            push_warning(msg)
    ```

    2. `apps/mobile/src/core/events/README.md`:
    ```markdown
    # events/

    Payloads tipados para eventos complexos entram aqui conforme necessário (ex.:
    `MatchStartedEvent` em GSD 06). Os eventos desta fase (`config_loaded`, `save_loaded`,
    etc., em `../event_bus.gd`) usam apenas tipos primitivos e não precisam de payload
    dedicado ainda.

    **Preenchido em:** conforme a necessidade, a partir de GSD 06.
    ```

    3. `apps/mobile/tests/unit/test_event_bus.gd` (GUT):
       - `test_subscribe_receives_emission`: conecta um `Callable` que seta uma flag local
         (ex.: `var received := false; func on_loaded(): received = true`), chama
         `bus.emit_config_loaded()`, assevera `received == true`.
       - `test_unsubscribe_stops_delivery`: conecta, desconecta, emite, assevera que a flag
         **não** foi setada.
       - `test_save_loaded_carries_result_value`: conecta um callback que guarda o argumento
         recebido, chama `bus.emit_save_loaded(SaveService.SaveResult.RESTORED_FROM_BACKUP)`,
         assevera que o valor guardado é `SaveService.SaveResult.RESTORED_FROM_BACKUP` (1).
       - `test_rate_limit_flagged_after_threshold`: chama `bus.emit_config_loaded()`
         `MAX_EMISSIONS_PER_SECOND + 1` vezes em sequência (mesmo frame de teste), assevera
         `bus.get_recent_emission_count("config_loaded") > EventBus.MAX_EMISSIONS_PER_SECOND`.
         (Se `Build.is_debug()` for `false` neste ambiente de teste — não deveria, já que GUT
         roda no binário debug — o teste deve pular com uma mensagem clara em vez de falhar
         silenciosamente; use `gut.p("skipped: not a debug build")` + `pending()` do GUT
         nesse caso.)
  </action>
  <verify>
    <automated>./tools/ci/test-client.sh</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/core/event_bus.gd` contains `class_name EventBus`
    - `apps/mobile/src/core/event_bus.gd` contains at least 4 `signal ` declarations
    - `apps/mobile/src/core/event_bus.gd` contains `MAX_EMISSIONS_PER_SECOND`
    - `apps/mobile/src/core/event_bus.gd` does NOT contain `emit(\"` or `.emit(\"` (no string-based generic emit)
    - `apps/mobile/tests/unit/test_event_bus.gd` contains exactly 4 `func test_` definitions
    - `./tools/ci/test-client.sh` exits 0 with these tests included
  </acceptance_criteria>
  <done>EventBus com 4 sinais tipados, assinatura/desassinatura funcionando, e detecção de emissão acima do limite em debug.</done>
</task>

</tasks>

<verification>
`./tools/ci/test-client.sh` inclui e passa os 4 testes de `test_event_bus.gd`. Nenhum sinal é
emitido via string genérica em nenhum arquivo desta fase.
</verification>

<success_criteria>
EventBus pronto para ser consumido por `Bootstrap` (Plano 01-10) e por qualquer fase futura
que precise notificar múltiplos ouvintes desacoplados sobre um evento raro — com uma trava
real (não só documental) contra abuso por frame.
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-06-SUMMARY.md`.
</output>
