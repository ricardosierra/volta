---
phase: 01-repository-foundation
plan: 3
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/mobile/addons/gut/
  - apps/mobile/.gutconfig.json
  - apps/mobile/tests/unit/test_example.gd
  - apps/mobile/tests/integration/test_example.gd
  - apps/mobile/tests/gameplay/test_example.gd
  - apps/mobile/.gitignore
  - tools/ci/test-client.sh
  - tools/README.md
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "É possível escrever e rodar um teste GUT headless desde este ponto da fase"
    - "./tools/ci/test-client.sh sai com código diferente de zero quando um teste falha, e zero quando todos passam"
    - "addons/gut nunca entra num build de release (fica fora do export)"
  artifacts:
    - path: "apps/mobile/addons/gut/"
      provides: "framework GUT pinado, isolado em addons/"
    - path: "tools/ci/test-client.sh"
      provides: "roda GUT headless e propaga o código de saída"
  key_links:
    - from: "tools/ci/test-client.sh"
      to: "apps/mobile/addons/gut/gut_cmdln.gd"
      via: "godot --headless -s addons/gut/gut_cmdln.gd"
      pattern: "gut_cmdln\\.gd"
---

<objective>
Instalar o GUT (REPO-006) para que testes possam ser escritos e rodados headless a partir
deste ponto da fase — pré-requisito de todo plano seguinte que promete um `<verify automated>`
real em vez de "MISSING".

Purpose: sem isto, nenhum outro plano desta fase consegue provar (automaticamente) que seus
testes passam.

Output: `apps/mobile/addons/gut/` funcional; `tools/ci/test-client.sh` verde/vermelho
corretamente, propagando o exit code.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/decisions/ADR-0013-testing-stack.md
@tools/README.md
@tests/README.md

<environment_facts>
Resolução do binário do Godot — mesmo padrão do Plano 01-01 (binário real em
`/Applications/Godot_CLI.app/Contents/MacOS/Godot`, não está em PATH como `godot`). Todo
script criado aqui deve resolver `GODOT_BIN` do mesmo jeito:
```bash
GODOT_BIN="${GODOT_BIN:-}"
if [ -z "$GODOT_BIN" ]; then
  if command -v godot >/dev/null 2>&1; then GODOT_BIN=godot
  elif [ -x "/Applications/Godot_CLI.app/Contents/MacOS/Godot" ]; then GODOT_BIN="/Applications/Godot_CLI.app/Contents/MacOS/Godot"
  else echo "ERRO: binário do Godot não encontrado. Defina GODOT_BIN." >&2; exit 1; fi
fi
```

**Versão pinada do GUT: `v9.7.1`** (última release estável de `bitwes/Gut` no momento deste
planejamento — 2026-07-10 — compatível com Godot 4.x). Não existe asset de release zipado;
baixe o tarball do tag e copie só a pasta `addons/gut`:
```bash
curl -L -o /tmp/gut-9.7.1.tar.gz https://github.com/bitwes/Gut/archive/refs/tags/v9.7.1.tar.gz
tar -xzf /tmp/gut-9.7.1.tar.gz -C /tmp
cp -R /tmp/Gut-9.7.1/addons/gut apps/mobile/addons/gut
rm -rf /tmp/gut-9.7.1.tar.gz /tmp/Gut-9.7.1
```
Se este passo estiver sendo executado depois de 2026-08-24 e uma versão mais nova e estável
tiver saído, prefira a mais nova release não-prerelease e registre a versão real usada no
SUMMARY (não silenciosamente troque sem documentar).
</environment_facts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Instalar GUT isolado em addons/</name>
  <files>apps/mobile/addons/gut/, apps/mobile/.gitignore</files>
  <read_first>
    - docs/decisions/ADR-0013-testing-stack.md (por que GUT, regra: isolado em addons/, versão pinada)
  </read_first>
  <action>
    1. Baixe e copie `addons/gut` conforme o bloco de comandos em `<environment_facts>`.
    2. Confirme que `apps/mobile/addons/gut/plugin.cfg` existe e contém uma linha
       `version=` — anote o valor exato no SUMMARY como a versão pinada real.
    3. Em `apps/mobile/.gitignore`, **não** ignore `addons/gut/` (ele precisa ser commitado —
       é a "versão pinada" isolada em `addons/`, não um download em build time). Confirme que
       nenhuma linha existente do `.gitignore` acidentalmente exclui `addons/`.
    4. Crie 3 testes de exemplo, um por pasta, cada um com um caso trivial que **passa**:
       - `apps/mobile/tests/unit/test_example.gd`:
       ```gdscript
       extends GutTest

       func test_arithmetic_sanity() -> void:
           assert_eq(1 + 1, 2)
       ```
       - `apps/mobile/tests/integration/test_example.gd`: mesmo padrão, nome
         `test_integration_sanity`.
       - `apps/mobile/tests/gameplay/test_example.gd`: mesmo padrão, nome
         `test_gameplay_sanity`.
  </action>
  <verify>
    <automated>test -f apps/mobile/addons/gut/gut_cmdln.gd && test -f apps/mobile/addons/gut/plugin.cfg && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/addons/gut/gut_cmdln.gd` existe
    - `apps/mobile/addons/gut/plugin.cfg` existe e contém `version=`
    - `apps/mobile/tests/unit/test_example.gd`, `apps/mobile/tests/integration/test_example.gd`, `apps/mobile/tests/gameplay/test_example.gd` existem, cada um com exatamente 1 `func test_`
    - `git check-ignore apps/mobile/addons/gut/gut_cmdln.gd` retorna código diferente de zero (ou seja, o arquivo NÃO está ignorado)
  </acceptance_criteria>
  <done>GUT instalado e commitável em apps/mobile/addons/gut/; 3 testes de exemplo passam trivialmente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: tools/ci/test-client.sh — GUT headless com exit code correto</name>
  <files>tools/ci/test-client.sh, apps/mobile/tests/unit/test_example.gd, tools/README.md</files>
  <read_first>
    - docs/decisions/ADR-0013-testing-stack.md (GUT roda com `--headless -s addons/gut/gut_cmdln.gd`)
    - tools/ci/validate-repo.sh (estilo de script já usado no repo — mesmas convenções de `set -uo pipefail`, `cd "$(dirname "$0")/../.."`, mensagens `OK:`/`FALHA:`)
  </read_first>
  <action>
    1. Crie `tools/ci/test-client.sh`:
    ```bash
    #!/usr/bin/env bash
    # Roda os testes GUT (unit + integration + gameplay) headless e propaga o exit code.
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1

    GODOT_BIN="${GODOT_BIN:-}"
    if [ -z "$GODOT_BIN" ]; then
      if command -v godot >/dev/null 2>&1; then GODOT_BIN=godot
      elif [ -x "/Applications/Godot_CLI.app/Contents/MacOS/Godot" ]; then GODOT_BIN="/Applications/Godot_CLI.app/Contents/MacOS/Godot"
      else echo "ERRO: binário do Godot não encontrado. Defina GODOT_BIN." >&2; exit 1
      fi
    fi

    "$GODOT_BIN" --headless --path apps/mobile -s addons/gut/gut_cmdln.gd -gdir=res://tests -gexit
    code=$?
    if [ $code -eq 0 ]; then
      echo "✅ test-client: todos os testes GUT passaram."
    else
      echo "❌ test-client: pelo menos um teste GUT falhou (exit $code)."
    fi
    exit $code
    ```
    2. Torne o script executável (`chmod +x tools/ci/test-client.sh`).
    3. **Prova de que o exit code é propagado** (obrigatório por REPO-006 / TASKS.md — "um
       teste que passa e um que falha propositalmente, removido depois de validar"):
       a. Adicione temporariamente a `apps/mobile/tests/unit/test_example.gd` um caso que
          falha de propósito: `func test_deliberately_fails() -> void: assert_eq(1, 2)`.
       b. Rode `./tools/ci/test-client.sh`; confirme `exit` diferente de zero.
       c. **Remova** o caso que falha (volte `test_example.gd` a conter só o caso que
          passa da Task 1). Rode `./tools/ci/test-client.sh` de novo; confirme `exit 0`.
       d. Registre os dois resultados (falha proposital confirmada, depois verde) no SUMMARY
          — não deixe o caso de falha no arquivo final.
    4. Atualize a linha correspondente em `tools/README.md` (`ci/test-client.sh`) removendo
       o "GSD 01 / REPO-006" pendente se a tabela usar esse marcador para indicar "ainda não
       criado" — deixe a linha descrevendo o script como existente.
  </action>
  <verify>
    <automated>./tools/ci/test-client.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/ci/test-client.sh` is executable (`test -x tools/ci/test-client.sh`)
    - `tools/ci/test-client.sh` contains `-gexit`
    - `tools/ci/test-client.sh` contains `exit $code`
    - Running `./tools/ci/test-client.sh` after Task 1's example tests exits 0
    - `apps/mobile/tests/unit/test_example.gd` does NOT contain `test_deliberately_fails` in the final committed state
  </acceptance_criteria>
  <done>test-client.sh roda GUT headless, propaga exit code corretamente (provado com um teste que falhou de propósito e foi removido), e termina verde nesta tarefa.</done>
</task>

</tasks>

<verification>
`./tools/ci/test-client.sh` roda os 3 testes de exemplo e sai 0. A prova de propagação de
exit code (falha proposital → não-zero → remoção → zero de novo) está documentada no SUMMARY.
`addons/gut` está commitado, não baixado em build time.
</verification>

<success_criteria>
A partir deste plano, todo `<verify><automated>` dos planos seguintes desta fase pode
referenciar `./tools/ci/test-client.sh` (ou uma chamada direta ao GUT filtrando por script)
como um comando real, não "MISSING".
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-03-SUMMARY.md`.
</output>
