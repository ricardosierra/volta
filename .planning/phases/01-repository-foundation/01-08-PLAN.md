---
phase: 01-repository-foundation
plan: 8
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - tools/ci/lint.sh
  - tools/ci/lint_docs.sh
  - tools/ci/lint_gdscript.sh
  - tests/tools/fixtures/lint/untyped_var.gd
  - tests/tools/fixtures/lint/untyped_param.gd
  - tests/tools/fixtures/lint/untyped_return.gd
  - tests/tools/run_lint_negative_checks.sh
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "Um arquivo GDScript sem tipagem estática (var sem tipo, parâmetro sem tipo, retorno sem tipo) faz lint.sh falhar"
    - "Um documento markdown sem título H1 no topo faz lint.sh falhar"
    - "lint.sh passa limpo no estado atual do repositório"
  artifacts:
    - path: "tools/ci/lint.sh"
      provides: "orquestra lint_gdscript.sh + lint_docs.sh, propaga exit code"
    - path: "tools/ci/lint_gdscript.sh"
      provides: "heurística de tipagem estática obrigatória (var/param/retorno)"
    - path: "tools/ci/lint_docs.sh"
      provides: "checagem de H1 no topo de cada .md de docs/ e .gsd/, usável isoladamente pelo validate.yml"
  key_links:
    - from: "tools/ci/lint.sh"
      to: "tools/ci/lint_gdscript.sh"
      via: "chamada direta"
      pattern: "lint_gdscript\\.sh"
    - from: "tools/ci/lint.sh"
      to: "tools/ci/lint_docs.sh"
      via: "chamada direta"
      pattern: "lint_docs\\.sh"
---

<objective>
Implementar `tools/ci/lint.sh` (REPO-010) — estilo consistente sem discussão em PR, com ênfase
na regra mais importante de CLAUDE.md: tipagem estática obrigatória em parâmetro, retorno e
membro, sem exceção.

Purpose: `validate-repo.sh` (Plano 01-07) verifica convenções estruturais (nomes, TODO, MOCK,
camadas); `lint.sh` verifica o **conteúdo** do código GDScript e a estrutura mínima dos docs.
São scripts complementares, não redundantes.

Output: `lint.sh` combinando `lint_gdscript.sh` (tipagem) + `lint_docs.sh` (título H1),
provado com fixtures que devem falhar.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONTRIBUTING.md
@docs/architecture/overview.md

<interfaces>
Este plano é bash puro — não depende de nenhum serviço GDScript de outro plano. A dependência
de `01-03` (GUT) existe só por ordenação declarada em `TASKS.md` (REPO-010 depende de
REPO-006), não por necessidade técnica real; documente essa ausência de acoplamento real no
SUMMARY se notar.

`lint_docs.sh` é criado como script **separado** e chamado tanto por `lint.sh` (que também
roda a checagem de GDScript) quanto, no Plano 01-09 (CI), diretamente por `validate.yml` — que
precisa rodar mesmo antes de o projeto Godot ter qualquer `.gd` significativo. Não funda os
dois scripts em um só.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: lint_gdscript.sh + lint_docs.sh + lint.sh</name>
  <files>tools/ci/lint.sh, tools/ci/lint_docs.sh, tools/ci/lint_gdscript.sh</files>
  <read_first>
    - CLAUDE.md regra 1 ("GDScript com TIPAGEM ESTÁTICA em parâmetro, retorno e membro. Sem exceção.")
    - tools/ci/validate-repo.sh (estilo/convenções do repo para scripts bash: `set -uo pipefail`, `fail()`/`ok()`/`note()`, `cd "$(dirname "$0")/../.."`)
  </read_first>
  <action>
    1. `tools/ci/lint_gdscript.sh` — heurística de tipagem estática (documentada como
       heurística, não parser completo). Exclua sempre `addons/` e
       `tests/tools/fixtures/` do escaneamento normal:
    ```bash
    #!/usr/bin/env bash
    # Heurística de tipagem estática obrigatória em GDScript (CLAUDE.md regra 1).
    # Não é um parser completo: cobre os 3 casos mais comuns de vazamento de tipagem.
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1

    FAIL=0
    GD_FILES=$(find apps packages services -name '*.gd' -not -path '*/addons/*' -not -path '*/tests/tools/fixtures/*' 2>/dev/null)

    # 1. var sem tipo (nem ": Type" nem ":=") — aceita "var x: int = 5" e "var x := 5", rejeita "var x = 5"
    untyped_var=$(echo "$GD_FILES" | xargs grep -nE '\bvar[[:space:]]+[A-Za-z_][A-Za-z0-9_]*[[:space:]]*=' 2>/dev/null | grep -vE ':=' || true)
    if [ -n "$untyped_var" ]; then
      echo "FALHA: var sem tipagem estática (nem ': Tipo' nem ':='):"
      echo "$untyped_var" | sed 's/^/    /'
      FAIL=1
    fi

    # 2. func sem "->" de retorno
    untyped_return=$(echo "$GD_FILES" | xargs grep -nE '^\s*(static\s+)?func\s+[A-Za-z_][A-Za-z0-9_]*\s*\([^)]*\)\s*:' 2>/dev/null | grep -v '\->' || true)
    if [ -n "$untyped_return" ]; then
      echo "FALHA: func sem tipo de retorno explícito ('-> Tipo'):"
      echo "$untyped_return" | sed 's/^/    /'
      FAIL=1
    fi

    # 3. parâmetro de func sem ":" (heurística: qualquer func com parênteses não-vazios
    #    onde algum parâmetro, separado por vírgula, não contém ":")
    untyped_param=$(echo "$GD_FILES" | while read -r f; do
      [ -z "$f" ] && continue
      grep -nE '^\s*(static\s+)?func\s+[A-Za-z_][A-Za-z0-9_]*\s*\(' "$f" | while IFS=: read -r lineno rest; do
        params=$(echo "$rest" | sed -E 's/^[^(]*\(([^)]*)\).*/\1/')
        [ -z "$params" ] && continue
        IFS=',' read -ra parts <<< "$params"
        for p in "${parts[@]}"; do
          trimmed=$(echo "$p" | sed -E 's/^[[:space:]]+|[[:space:]]+$//g')
          [ -z "$trimmed" ] && continue
          if ! echo "$trimmed" | grep -q ':'; then
            echo "$f:$lineno: parâmetro sem tipo: '$trimmed'"
          fi
        done
      done
    done)
    if [ -n "$untyped_param" ]; then
      echo "FALHA: parâmetro de função sem tipo explícito:"
      echo "$untyped_param" | sed 's/^/    /'
      FAIL=1
    fi

    [ $FAIL -eq 0 ] && echo "OK: tipagem estática — nenhuma violação encontrada"
    exit $FAIL
    ```

    2. `tools/ci/lint_docs.sh`:
    ```bash
    #!/usr/bin/env bash
    # Cada .md de docs/ e .gsd/ deve começar (primeira linha não-vazia) com um título "# ".
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1

    FAIL=0
    for f in $(find docs .gsd -name '*.md' 2>/dev/null); do
      first_line=$(grep -m1 -v '^[[:space:]]*$' "$f" || true)
      case "$first_line" in
        "# "*) ;;
        *) echo "FALHA: $f não começa com um título H1 ('# ...'): '$first_line'"; FAIL=1 ;;
      esac
    done
    [ $FAIL -eq 0 ] && echo "OK: todos os docs começam com título H1"
    exit $FAIL
    ```

    3. `tools/ci/lint.sh`:
    ```bash
    #!/usr/bin/env bash
    # Lint de GDScript (tipagem estática) + lint de docs (título H1). Reúne e propaga exit code.
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1

    FAIL=0
    ./tools/ci/lint_gdscript.sh || FAIL=1
    ./tools/ci/lint_docs.sh || FAIL=1

    echo ""
    if [ $FAIL -eq 0 ]; then echo "✅ lint: tudo certo."; else echo "❌ lint: corrija os itens acima."; fi
    exit $FAIL
    ```
       Torne os 3 scripts executáveis (`chmod +x`).
  </action>
  <verify>
    <automated>./tools/ci/lint.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/ci/lint.sh`, `tools/ci/lint_gdscript.sh`, `tools/ci/lint_docs.sh` are all executable
    - `./tools/ci/lint.sh` exits 0 on the current repository state (all `.gd` files created by earlier plans in this phase already use static typing; all `.md` files already start with `# `)
    - `tools/ci/lint_gdscript.sh` contains three distinct FALHA branches (var, return, param)
  </acceptance_criteria>
  <done>lint.sh roda limpo no estado atual do repositório, combinando checagem de tipagem GDScript e estrutura de docs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Provar lint.sh com fixtures que devem falhar</name>
  <files>
    tests/tools/fixtures/lint/untyped_var.gd, tests/tools/fixtures/lint/untyped_param.gd,
    tests/tools/fixtures/lint/untyped_return.gd, tests/tools/run_lint_negative_checks.sh
  </files>
  <read_first>
    - tools/ci/lint_gdscript.sh (versão criada na Task 1 — confirme as mensagens de FALHA exatas antes de escrever os asserts)
  </read_first>
  <action>
    1. Crie as 3 fixtures estáticas em `tests/tools/fixtures/lint/` (esta pasta já é excluída
       do escaneamento normal de `lint_gdscript.sh` pela Task 1):
       - `untyped_var.gd`: `class_name FixtureUntypedVar\nextends RefCounted\nvar broken = 5\n`
       - `untyped_param.gd`: `class_name FixtureUntypedParam\nextends RefCounted\nfunc broken(x) -> void:\n    pass\n`
       - `untyped_return.gd`: `class_name FixtureUntypedReturn\nextends RefCounted\nfunc broken():\n    pass\n`

    2. `tests/tools/run_lint_negative_checks.sh` — mesmo padrão de injeção/remoção do Plano
       01-07 (`tests/tools/run_negative_checks.sh`), mas para `lint.sh`:
    ```bash
    #!/usr/bin/env bash
    # Prova que lint.sh detecta de verdade a ausência de tipagem estática.
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1

    FAIL=0
    INJECTED=()
    cleanup() { for f in "${INJECTED[@]}"; do rm -f "$f"; done; }
    trap cleanup EXIT

    check_case() {
      local desc="$1" src_fixture="$2" dest="$3" expect_grep="$4"
      cp "$src_fixture" "$dest"
      INJECTED+=("$dest")
      local out; out=$(./tools/ci/lint.sh 2>&1); local code=$?
      rm -f "$dest"
      if [ "$code" -eq 0 ]; then echo "FALHA NO TESTE NEGATIVO: '$desc' não fez lint.sh falhar."; FAIL=1; return; fi
      if ! echo "$out" | grep -q "$expect_grep"; then echo "FALHA NO TESTE NEGATIVO: '$desc' falhou sem a mensagem esperada."; FAIL=1; return; fi
      echo "OK (negativo provado): $desc"
    }

    check_case "var sem tipagem estática" tests/tools/fixtures/lint/untyped_var.gd apps/mobile/src/core/__negtest_lint_var.gd "sem tipagem estática"
    check_case "parâmetro sem tipo" tests/tools/fixtures/lint/untyped_param.gd apps/mobile/src/core/__negtest_lint_param.gd "parâmetro"
    check_case "retorno sem tipo" tests/tools/fixtures/lint/untyped_return.gd apps/mobile/src/core/__negtest_lint_return.gd "tipo de retorno"

    echo ""
    if [ $FAIL -eq 0 ]; then echo "✅ os 3 casos negativos de lint provaram a falha esperada."; else echo "❌ pelo menos um caso negativo de lint não provou a falha."; fi

    final_out=$(./tools/ci/lint.sh 2>&1); final_code=$?
    if [ $final_code -ne 0 ]; then echo "❌ lint.sh não voltou a passar limpo:"; echo "$final_out"; FAIL=1
    else echo "✅ lint.sh volta a passar limpo depois da limpeza."; fi

    exit $FAIL
    ```
       Torne executável.
  </action>
  <verify>
    <automated>./tests/tools/run_lint_negative_checks.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tools/run_lint_negative_checks.sh` is executable
    - `tests/tools/run_lint_negative_checks.sh` contains exactly 3 calls to `check_case`
    - Running `./tests/tools/run_lint_negative_checks.sh` prints `✅ os 3 casos negativos de lint provaram a falha esperada.`
    - Running `./tests/tools/run_lint_negative_checks.sh` prints `✅ lint.sh volta a passar limpo depois da limpeza.` and exits 0
    - After running the script, `git status --porcelain` shows no `__negtest_lint_` files left behind
  </acceptance_criteria>
  <done>3 fixtures provam que lint.sh detecta ausência de tipagem estática (var, param, retorno); lint.sh volta a passar limpo depois.</done>
</task>

</tasks>

<verification>
`./tools/ci/lint.sh` passa limpo no estado normal do repositório.
`./tests/tools/run_lint_negative_checks.sh` prova as 3 violações de tipagem e o título H1 de
docs continua garantido pelo `lint_docs.sh`, deixando o repositório limpo ao final.
</verification>

<success_criteria>
ACCEPTANCE A01-10 satisfeito: "lint.sh reprova arquivo sem tipagem estática" — provado, não
presumido, com 3 casos negativos reais.
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-08-SUMMARY.md`.
</output>
