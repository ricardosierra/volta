---
phase: 01-repository-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .godot-version
  - apps/mobile/project.godot
  - apps/mobile/.gitignore
  - apps/mobile/README.md
  - apps/mobile/src/core/README.md
  - apps/mobile/src/gameplay/README.md
  - apps/mobile/src/territory/README.md
  - apps/mobile/src/runner/README.md
  - apps/mobile/src/ai/README.md
  - apps/mobile/src/arena/README.md
  - apps/mobile/src/input/README.md
  - apps/mobile/src/presentation/README.md
  - apps/mobile/src/ui/README.md
  - apps/mobile/src/progression/README.md
  - apps/mobile/src/platform/README.md
  - apps/mobile/scenes/README.md
  - apps/mobile/resources/README.md
  - apps/mobile/assets/README.md
  - apps/mobile/tests/unit/README.md
  - apps/mobile/tests/integration/README.md
  - apps/mobile/tests/gameplay/README.md
  - apps/mobile/src/core/build_flags.gd
  - apps/mobile/src/core/build.gd
  - apps/mobile/resources/build_flags.tres
  - apps/mobile/tests/unit/README.md
  - docs/architecture/debug-tools.md
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "Um Godot 4.3 abre apps/mobile sem erro nem warning, na versão pinada em .godot-version"
    - "A estrutura de pastas de src/ existe exatamente como docs/architecture/overview.md §8 descreve, cada uma com um README curto"
    - "Existe um único ponto de verdade sobre debug vs release (Build.is_debug()), testável sem depender de um build de release real"
  artifacts:
    - path: "apps/mobile/project.godot"
      provides: "projeto Godot 4.3 configurado para mobile (renderer, resolução, stretch, orientação, tick fixo)"
    - path: ".godot-version"
      provides: "versão pinada da engine, lida por doctor.sh e pelo CI"
    - path: "apps/mobile/src/core/build.gd"
      provides: "Build.is_debug() / Build.version() / Build.commit() / Build.env()"
    - path: "apps/mobile/src/core/build_flags.gd"
      provides: "BuildFlags Resource (debug_tools_enabled, commit_sha)"
  key_links:
    - from: "apps/mobile/src/core/build.gd"
      to: "apps/mobile/resources/build_flags.tres"
      via: "ResourceLoader.load(FLAGS_PATH)"
      pattern: "ResourceLoader\\.load\\(FLAGS_PATH\\)"
---

<objective>
Criar o projeto Godot 4.3 em `apps/mobile` com a engine pinada e as configurações de mobile
corretas (REPO-001), e implementar `Build` — o único ponto de verdade sobre debug vs release
(REPO-002).

Purpose: tudo nas próximas 10 fases depende de o projeto Godot abrir e rodar. Esta é a base
literal — nenhuma outra tarefa desta fase (nem de nenhuma fase seguinte) pode começar antes
dela.

Output: `apps/mobile/` com `project.godot` funcional, estrutura de `src/` documentada, e
`Build` testável.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-repository-foundation/01-CONTEXT.md
@docs/architecture/overview.md
@docs/decisions/ADR-0001-engine.md
@docs/decisions/ADR-0014-simulation-tick-model.md
@CLAUDE.md
@CONTRIBUTING.md
@.gsd/phases/01-repository-foundation/TASKS.md

<environment_facts>
Este ambiente JÁ TEM o Godot 4.3 instalado, mas **não está no PATH** como `godot`:

- Binário real: `/Applications/Godot_CLI.app/Contents/MacOS/Godot`
- Versão relatada por `--version`: `4.3.stable.official.77dcf97d8` (contém a substring
  `4.3.stable`, que é o que `.godot-version` deve conter e o que `tools/dev/doctor.sh` já
  compara por substring).
- Export templates 4.3.stable **já instalados** em
  `~/Library/Application Support/Godot/export_templates/4.3.stable` — não é preciso instalar
  nada agora. Se ao rodar esta tarefa eles não estiverem lá, documente o passo de instalação
  em `docs/mobile/android.md` sob um novo `## Setup local` e registre a lacuna no SUMMARY —
  não invente um resultado de verificação.

**Todo script criado nesta fase que invoca o Godot deve resolver o binário assim** (nunca
hardcode `godot` sozinho, nem hardcode o caminho do `.app`):

```bash
resolve_godot() {
  if [ -n "${GODOT_BIN:-}" ]; then echo "$GODOT_BIN"; return 0; fi
  if command -v godot >/dev/null 2>&1; then echo "godot"; return 0; fi
  for candidate in "/Applications/Godot_CLI.app/Contents/MacOS/Godot" "/Applications/Godot.app/Contents/MacOS/Godot"; do
    [ -x "$candidate" ] && { echo "$candidate"; return 0; }
  done
  return 1
}
GODOT_BIN="$(resolve_godot)" || { echo "ERRO: binário do Godot não encontrado. Defina GODOT_BIN." >&2; exit 1; }
```

Para verificações manuais ad-hoc nesta tarefa (não em script comitado), resolva o binário da
mesma forma antes de chamar `--headless --check-only`.
</environment_facts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold do projeto Godot 4.3 + estrutura de src/</name>
  <files>
    apps/mobile/project.godot, .godot-version, apps/mobile/.gitignore, apps/mobile/README.md,
    apps/mobile/src/{core,gameplay,territory,runner,ai,arena,input,presentation,ui,progression,platform}/README.md,
    apps/mobile/scenes/README.md, apps/mobile/resources/README.md, apps/mobile/assets/README.md,
    apps/mobile/tests/{unit,integration,gameplay}/README.md
  </files>
  <read_first>
    - docs/architecture/overview.md (§8 — estrutura exata de pastas; §5 — convenções)
    - docs/decisions/ADR-0001-engine.md (por que 4.3, renderer Mobile)
    - docs/decisions/ADR-0014-simulation-tick-model.md (por que physics_ticks_per_second=60)
    - apps/mobile/README.md (estado atual — placeholder "vazio por enquanto")
    - tools/dev/doctor.sh (como ele lê .godot-version e compara com `godot --version`)
  </read_first>
  <action>
    1. Escreva `.godot-version` na raiz do repo contendo exatamente `4.3.stable` (uma linha,
       sem `v` na frente, sem sufixo `.official...`).

    2. Crie `apps/mobile/project.godot` com **exatamente** estas seções e chaves (sintaxe
       Godot 4.3, `config_version=5`):

    ```ini
    config_version=5

    [application]

    config/name="VOLTA"
    config/version="0.1.0"
    config/features=PackedStringArray("4.3", "Mobile")
    run/main_scene=""

    [autoload]

    Log="*res://src/core/log/log.gd"

    [display]

    window/size/viewport_width=1080
    window/size/viewport_height=1920
    window/stretch/mode="canvas_items"
    window/stretch/aspect="expand"
    window/handheld/orientation="portrait"

    [physics]

    common/physics_ticks_per_second=60
    common/max_physics_steps_per_frame=4

    [rendering]

    renderer/rendering_method="mobile"
    ```

       Notas obrigatórias:
       - `run/main_scene` fica **vazio** por enquanto: `scenes/boot.tscn` só é criado no
         Plano 01-02 (ele preencherá este valor). Um `run/main_scene` vazio não deve fazer
         `--check-only` falhar.
       - A seção `[autoload]` já registra `Log` **agora**, mesmo que `src/core/log/log.gd`
         só seja criado no Plano 01-02 — isso é esperado: `--check-only` desta tarefa vai
         reportar que o script do autoload não existe ainda. **Não** rode `--check-only`
         cobrando zero warning até o Plano 01-02 estar pronto; a verificação desta tarefa
         especificamente cobre apenas que o projeto abre e a estrutura existe (ver `<verify>`
         abaixo, que usa `--headless --check-only` mas tolera o aviso específico de autoload
         ausente, documentado explicitamente no SUMMARY).
       - `Bootstrap` **não** é autoload — ver decisão registrada no Plano 01-02 (ele é a cena
         principal, não um singleton Godot). Não adicione `Bootstrap=` a `[autoload]` aqui
         nem depois.

    3. Crie `apps/mobile/.gitignore`:
    ```
    .godot/
    .import/
    export_presets.cfg
    android/build/
    *.translation
    ```

    4. Crie a estrutura vazia de `src/` com um `README.md` por pasta. Use este conteúdo (uma
       tabela — copie a coluna certa para cada arquivo, formato livre de 1-2 frases):

    | Pasta | Conteúdo do README.md |
    |---|---|
    | `src/core/README.md` | `# core/\n\nbootstrap, event_bus, config, log, save, registry, build — a fundação de que todo o resto depende.\n\n**Preenchido em:** GSD 01.` |
    | `src/gameplay/README.md` | `# gameplay/\n\nmatch_director, game_state, modes/, score/, rules/ — orquestra a partida sobre a simulação.\n\n**Preenchido em:** GSD 06.` |
    | `src/territory/README.md` | `# territory/\n\ngrid.gd, claim_map.gd, arc_tracker.gd, seal_solver.gd — o motor de território (grid denso + flood fill).\n\n**Preenchido em:** GSD 03.` |
    | `src/runner/README.md` | `# runner/\n\nrunner.gd, runner_state.gd, movement.gd, stat_block.gd — estado, posição, colisão do Runner.\n\n**Preenchido em:** GSD 02.` |
    | `src/ai/README.md` | `# ai/\n\nbot_brain.gd, actions/, perception/, profiles/ — IA por utilidade dos bots.\n\n**Preenchido em:** GSD 05.` |
    | `src/arena/README.md` | `# arena/\n\narena_definition.gd, spawner.gd, modifiers/ — definição de arena, spawns e modificadores.\n\n**Preenchido em:** GSD 02.` |
    | `src/input/README.md` | `# input/\n\ninput_router.gd, drivers/ — drivers de input traduzidos em direção desejada.\n\n**Preenchido em:** GSD 02.` |
    | `src/presentation/README.md` | `# presentation/\n\ncamera/, vfx/, audio/, haptics/, territory_renderer.gd — tudo que só LÊ a simulação. Nunca importado por territory/, runner/, ai/ ou gameplay/.\n\n**Preenchido em:** GSD 09.` |
    | `src/ui/README.md` | `# ui/\n\nscreens/, hud/, design_system/, components/ — telas, HUD e design system.\n\n**Preenchido em:** GSD 07.` |
    | `src/progression/README.md` | `# progression/\n\nprofile.gd, xp.gd, achievements/, challenges/, cosmetics/ — perfil e desbloqueios.\n\n**Preenchido em:** GSD 10.` |
    | `src/platform/README.md` | `# platform/\n\nanalytics/, telemetry/, remote_config/, store/ — fala com o mundo externo por interface própria.\n\n**Preenchido em:** GSD 15.` |

       Também crie:
       - `apps/mobile/scenes/README.md` — "Cenas `.tscn` organizadas espelhando `src/`. Preenchido em: GSD 01+."
       - `apps/mobile/resources/README.md` — "`.tres` de balance, temas, modos, perfis de bot — sincronizados de `packages/shared/config` por `tools/dev/sync_config.sh`. Preenchido em: GSD 01."
       - `apps/mobile/assets/README.md` — "Arte e áudio já importados/processados pelo Godot (fonte em `/assets` na raiz). Preenchido em: GSD 08."
       - `apps/mobile/tests/unit/README.md` — "Testes unitários GUT — uma classe, sem cena, sem I/O real. Preenchido em: GSD 01+."
       - `apps/mobile/tests/integration/README.md` — "Testes GUT que cobrem fluxo entre 2+ sistemas (ex.: boot completo). Preenchido em: GSD 01+."
       - `apps/mobile/tests/gameplay/README.md` — "Testes GUT que rodam a simulação sem nós visuais. Preenchido em: GSD 03+."

    5. Atualize `apps/mobile/README.md`: remova a frase "Vazio por enquanto" (não é mais
       verdade) e substitua por um parágrafo curto confirmando que o projeto está scaffolded,
       mantendo o link para `docs/architecture/overview.md`.
  </action>
  <verify>
    <automated>GODOT_BIN=$(command -v godot || echo "/Applications/Godot_CLI.app/Contents/MacOS/Godot"); "$GODOT_BIN" --headless --path apps/mobile --check-only; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cat .godot-version` produz exatamente `4.3.stable`
    - `grep -c 'renderer/rendering_method="mobile"' apps/mobile/project.godot` retorna 1
    - `grep -c 'window/size/viewport_width=1080' apps/mobile/project.godot` retorna 1
    - `grep -c 'window/size/viewport_height=1920' apps/mobile/project.godot` retorna 1
    - `grep -c 'window/stretch/mode="canvas_items"' apps/mobile/project.godot` retorna 1
    - `grep -c 'window/stretch/aspect="expand"' apps/mobile/project.godot` retorna 1
    - `grep -c 'window/handheld/orientation="portrait"' apps/mobile/project.godot` retorna 1
    - `grep -c 'common/physics_ticks_per_second=60' apps/mobile/project.godot` retorna 1
    - `grep -c 'common/max_physics_steps_per_frame=4' apps/mobile/project.godot` retorna 1
    - `grep -c '^Bootstrap=' apps/mobile/project.godot` retorna 0 (Bootstrap não é autoload)
    - Todas as 11 pastas de `apps/mobile/src/*/README.md` existem (`find apps/mobile/src -maxdepth 1 -type d | wc -l` = 12, contando `src` em si)
    - `apps/mobile/tests/unit/README.md`, `apps/mobile/tests/integration/README.md`, `apps/mobile/tests/gameplay/README.md` existem
    - `--headless --check-only` roda até o fim (não trava); qualquer aviso sobre o autoload `Log` ausente é esperado nesta tarefa e deve ser citado no SUMMARY, não escondido
  </acceptance_criteria>
  <done>project.godot existe com as chaves exatas acima; .godot-version = 4.3.stable; estrutura de src/ completa com READMEs; apps/mobile/README.md atualizado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build — ponto único de verdade sobre debug vs release</name>
  <files>
    apps/mobile/src/core/build_flags.gd, apps/mobile/src/core/build.gd,
    apps/mobile/resources/build_flags.tres, apps/mobile/tests/unit/test_build.gd,
    docs/architecture/debug-tools.md
  </files>
  <read_first>
    - docs/architecture/debug-tools.md (regra: "todo código de debug fica atrás de Build.is_debug()")
    - .env.example (VOLTA_DEBUG_TOOLS, VOLTA_ENV — os nomes das variáveis de referência)
    - CLAUDE.md regra 1 (tipagem estática obrigatória, sem exceção)
  </read_first>
  <behavior>
    - `Build.version()` retorna exatamente `"0.1.0"` (lido de `ProjectSettings.get_setting("application/config/version")`, que Task 1 já configurou).
    - `Build.env()` retorna `OS.get_environment("VOLTA_ENV")` quando a variável existe; caso contrário retorna `"development"` (default de `.env.example`).
    - `Build.commit()` retorna `BuildFlags.commit_sha`, que por padrão (sem override) é `"dev"`.
    - `Build.is_debug()` é `true` quando `OS.is_debug_build()` é `true` **e** `BuildFlags.debug_tools_enabled` é `true`; é `false` se qualquer um dos dois for `false`. Como não é possível alternar `OS.is_debug_build()` de dentro de um teste GUT (ele reflete o binário real usado para rodar o teste, que é sempre debug), a lógica pura de decisão fica isolada numa função testável sem depender do ambiente real: `Build._compute_is_debug(os_debug: bool, flags_enabled: bool) -> bool`, chamada por `is_debug()` com os valores reais. O teste cobre as 4 combinações de `_compute_is_debug`, cumprindo "is_debug() em debug e release" do TESTS.md sem precisar de um binário de release de verdade.
  </behavior>
  <action>
    1. Crie `apps/mobile/src/core/build_flags.gd`:
    ```gdscript
    class_name BuildFlags
    extends Resource

    ## Gerado/sobrescrito pelo pipeline de export para release (GSD 21 / ANDR-001).
    ## Em builds de debug e no editor, os valores abaixo (defaults do .tres commitado) valem.
    @export var debug_tools_enabled: bool = true
    @export var commit_sha: String = "dev"
    ```

    2. Salve uma instância padrão em `apps/mobile/resources/build_flags.tres` com
       `debug_tools_enabled = true` e `commit_sha = "dev"` (os defaults acima — pode ser
       criado pelo editor do Godot ou escrito à mão no formato `.tres` padrão do Godot 4,
       com `[gd_resource type="Resource" script_class="BuildFlags" ...]`).

    3. Crie `apps/mobile/src/core/build.gd`:
    ```gdscript
    class_name Build
    extends RefCounted

    const FLAGS_PATH: String = "res://resources/build_flags.tres"

    static var _flags_cache: BuildFlags = null

    static func is_debug() -> bool:
        return _compute_is_debug(OS.is_debug_build(), _get_flags().debug_tools_enabled)

    static func version() -> String:
        return ProjectSettings.get_setting("application/config/version", "0.0.0")

    static func commit() -> String:
        return _get_flags().commit_sha

    static func env() -> String:
        if OS.has_environment("VOLTA_ENV"):
            return OS.get_environment("VOLTA_ENV")
        return "development"

    static func _compute_is_debug(os_debug: bool, flags_enabled: bool) -> bool:
        return os_debug and flags_enabled

    static func _get_flags() -> BuildFlags:
        if _flags_cache == null:
            var loaded: Resource = ResourceLoader.load(FLAGS_PATH)
            _flags_cache = loaded as BuildFlags
            if _flags_cache == null:
                _flags_cache = BuildFlags.new()
        return _flags_cache
    ```
       Lembrete de convenção (`docs/architecture/overview.md` §5): ordem `class_name` →
       `extends` → docstring → `const` → vars → públicos → privados (`_prefixo`). Tipagem
       estática em todo parâmetro, retorno e membro — sem exceção (CLAUDE.md regra 1).

    4. Crie `apps/mobile/tests/unit/test_build.gd` (GUT — `extends GutTest`) com estes casos:
       - `test_version_matches_project_settings`: `assert_eq(Build.version(), "0.1.0")`
       - `test_commit_defaults_to_dev`: `assert_eq(Build.commit(), "dev")`
       - `test_is_debug_true_when_os_debug_and_flags_enabled`: `assert_true(Build._compute_is_debug(true, true))`
       - `test_is_debug_false_when_os_not_debug`: `assert_false(Build._compute_is_debug(false, true))`
       - `test_is_debug_false_when_flags_disabled`: `assert_false(Build._compute_is_debug(true, false))`
       - `test_is_debug_false_when_both_false`: `assert_false(Build._compute_is_debug(false, false))`
       (Estes testes não rodam ainda via `test-client.sh` — o GUT só é instalado no Plano
       01-03. Nesta tarefa, escreva o arquivo e valide a sintaxe com
       `--headless --path apps/mobile --check-only`; a execução real do GUT acontece quando
       o Plano 01-03 e os planos seguintes existirem. Registre isso no SUMMARY.)

    5. Em `docs/architecture/debug-tools.md`, adicione ao final uma seção curta:
    ```markdown
    ## API de Build (GSD 01)

    `Build.is_debug()` / `Build.version()` / `Build.commit()` / `Build.env()` — implementado
    em `apps/mobile/src/core/build.gd`. `debug_tools_enabled` vem de
    `apps/mobile/resources/build_flags.tres` (`BuildFlags`), regenerado com `false` no
    export de release (# TODO(GSD-21/ANDR-001): regenerar build_flags.tres com
    debug_tools_enabled=false para export de release).
    ```
  </action>
  <verify>
    <automated>GODOT_BIN=$(command -v godot || echo "/Applications/Godot_CLI.app/Contents/MacOS/Godot"); "$GODOT_BIN" --headless --path apps/mobile --check-only; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/core/build.gd` contains `class_name Build`
    - `apps/mobile/src/core/build.gd` contains `static func is_debug() -> bool`
    - `apps/mobile/src/core/build.gd` contains `static func _compute_is_debug(os_debug: bool, flags_enabled: bool) -> bool`
    - `apps/mobile/src/core/build_flags.gd` contains `class_name BuildFlags`
    - `apps/mobile/tests/unit/test_build.gd` contains exactly 6 lines matching `func test_`
    - `docs/architecture/debug-tools.md` contains `TODO(GSD-21/ANDR-001)`
    - `--headless --check-only` não reporta erro de sintaxe nos arquivos criados nesta tarefa
  </acceptance_criteria>
  <done>Build.is_debug()/version()/commit()/env() implementados e testáveis; BuildFlags.tres commitado; docs atualizada com o TODO correto de release.</done>
</task>

</tasks>

<verification>
`apps/mobile/project.godot` abre sem erro estrutural via `--check-only` (avisos sobre o
autoload `Log` ainda ausente são esperados e resolvidos no Plano 01-02). `.godot-version`
contém `4.3.stable`. `Build` existe, é testável de forma determinística via
`_compute_is_debug`, e não depende de nenhum outro serviço desta fase.
</verification>

<success_criteria>
Projeto Godot 4.3 scaffolded com renderer mobile, resolução 1080×1920, stretch
canvas_items/expand, portrait travado, tick fixo 60 Hz — exatamente como
`docs/decisions/ADR-0001-engine.md` e `ADR-0014` exigem. `Build` é o único ponto de verdade
sobre debug/release, pronto para ser usado por `Log`, `EventBus` e o menu de debug em fases
futuras.
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-01-SUMMARY.md`.
</output>
