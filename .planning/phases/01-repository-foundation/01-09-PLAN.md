---
phase: 01-repository-foundation
plan: 9
type: execute
wave: 4
depends_on: ["01-04", "01-05", "01-06", "01-07", "01-08"]
files_modified:
  - tools/ci/setup_godot.sh
  - .github/workflows/validate.yml
  - .github/workflows/client-ci.yml
  - README.md
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "validate.yml roda validate-repo.sh + lint de docs em todo push, sem precisar do projeto Godot"
    - "client-ci.yml instala o Godot pinado, importa assets, faz lint completo e roda os testes GUT"
    - "setup_godot.sh baixa exatamente a versão de .godot-version, com cache"
  artifacts:
    - path: "tools/ci/setup_godot.sh"
      provides: "instala editor + export templates da versão pinada, com cache"
    - path: ".github/workflows/client-ci.yml"
      provides: "pipeline completo do cliente (import, lint, testes GUT)"
  key_links:
    - from: ".github/workflows/client-ci.yml"
      to: "tools/ci/setup_godot.sh"
      via: "run: ./tools/ci/setup_godot.sh \"$(cat .godot-version)\""
      pattern: "setup_godot\\.sh"
    - from: ".github/workflows/validate.yml"
      to: "tools/ci/lint_docs.sh"
      via: "run: ./tools/ci/lint_docs.sh"
      pattern: "lint_docs\\.sh"
---

<objective>
Fazer tudo o que os planos 01-01 a 01-08 construíram rodar sozinho em todo push (REPO-011):
`setup_godot.sh` instala a engine pinada com cache; `validate.yml` cobre estrutura/convenções
mesmo sem o Godot instalado; `client-ci.yml` roda o pipeline completo do cliente.

Purpose: um repositório com scripts corretos mas sem CI automatizado ainda depende de alguém
lembrar de rodar os scripts manualmente. Esta é a última etapa antes do gate de merge real.

Output: `setup_godot.sh` funcional; `validate.yml` estendido com lint de docs; `client-ci.yml`
com Godot instalado via cache e todos os passos guardados.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/validate.yml
@.github/workflows/client-ci.yml
@tools/README.md

<interfaces>
`.github/workflows/validate.yml` e `.github/workflows/client-ci.yml` **já existem** (criados
no bootstrap GSD 00) — leia os dois antes de editar, as mudanças abaixo são incrementais.
`client-ci.yml` já referencia `./tools/ci/setup_godot.sh "$(cat .godot-version)"`,
`./tools/ci/lint.sh`, `./tools/ci/test-client.sh` e `./tools/dev/simulate.sh 20` — o último
não existe ainda (só chega em GSD 03), então mantenha o guard de "Projeto Godot existe?" já
presente, mas **remova ou marque como `continue-on-error: true`** o passo
"Amostra de gameplay" especificamente, já que `tools/dev/simulate.sh` não existe nesta fase e
não deve quebrar o CI da Fase 1.

Não existe remote `git` configurado neste repositório ainda (`git remote -v` vazio) — portanto
o "gate de merge com os dois workflows obrigatórios" (branch protection no GitHub) **não pode
ser configurado agora** via `gh api`. Documente isso claramente no SUMMARY como pendência
explícita para quando o repositório for publicado no GitHub — não tente criar o repositório
remoto sozinho (decisão do usuário, fora do escopo desta tarefa).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: setup_godot.sh — instala a engine pinada com cache</name>
  <files>tools/ci/setup_godot.sh</files>
  <read_first>
    - .godot-version (versão exata a instalar)
    - .github/workflows/client-ci.yml (como o script é chamado: `./tools/ci/setup_godot.sh "$(cat .godot-version)"`)
  </read_first>
  <action>
    Crie `tools/ci/setup_godot.sh`, recebendo a versão como `$1` (ex.: `4.3.stable`):
    ```bash
    #!/usr/bin/env bash
    # Instala o editor Godot (headless) + export templates da versão dada, com cache local.
    # Uso: ./tools/ci/setup_godot.sh 4.3.stable
    set -uo pipefail

    VERSION="${1:?uso: setup_godot.sh <versao, ex. 4.3.stable>}"
    CACHE_DIR="${GODOT_CACHE_DIR:-$HOME/.cache/godot-ci/$VERSION}"
    BIN_PATH="$CACHE_DIR/Godot_v${VERSION}_linux.x86_64"
    TEMPLATES_DIR="$HOME/.local/share/godot/export_templates/${VERSION}"

    mkdir -p "$CACHE_DIR"

    if [ -x "$BIN_PATH" ]; then
      echo "OK: Godot $VERSION já em cache ($BIN_PATH)"
    else
      echo "Baixando Godot $VERSION (editor headless, Linux)..."
      URL="https://github.com/godotengine/godot/releases/download/${VERSION/.stable/-stable}/Godot_v${VERSION}_linux.x86_64.zip"
      curl -fL -o "$CACHE_DIR/godot.zip" "$URL"
      unzip -o -q "$CACHE_DIR/godot.zip" -d "$CACHE_DIR"
      chmod +x "$BIN_PATH"
    fi

    if [ -d "$TEMPLATES_DIR" ] && [ -n "$(ls -A "$TEMPLATES_DIR" 2>/dev/null)" ]; then
      echo "OK: export templates $VERSION já em cache"
    else
      echo "Baixando export templates $VERSION..."
      TPL_URL="https://github.com/godotengine/godot/releases/download/${VERSION/.stable/-stable}/Godot_v${VERSION}_export_templates.tpz"
      mkdir -p "$TEMPLATES_DIR"
      curl -fL -o "$CACHE_DIR/templates.tpz" "$TPL_URL"
      unzip -o -q "$CACHE_DIR/templates.tpz" -d "$CACHE_DIR/templates_tmp"
      cp -R "$CACHE_DIR"/templates_tmp/templates/. "$TEMPLATES_DIR"/
      rm -rf "$CACHE_DIR/templates_tmp"
    fi

    mkdir -p "$HOME/.local/bin"
    ln -sf "$BIN_PATH" "$HOME/.local/bin/godot"
    echo "$HOME/.local/bin" >> "${GITHUB_PATH:-/dev/null}" 2>/dev/null || true
    echo "GODOT_BIN=$BIN_PATH" >> "${GITHUB_ENV:-/dev/null}" 2>/dev/null || true
    echo "OK: godot disponível em $HOME/.local/bin/godot (GODOT_BIN=$BIN_PATH)"
    ```
       Torne executável. Este script roda em Linux (runner `ubuntu-latest` do GH Actions) —
       não precisa resolver o binário local do macOS deste ambiente de desenvolvimento (essa
       resolução, feita nos outros scripts desta fase via `command -v godot` / caminho do
       `.app`, continua sendo o caminho usado localmente).
  </action>
  <verify>
    <automated>test -x tools/ci/setup_godot.sh && bash -n tools/ci/setup_godot.sh && echo "syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/ci/setup_godot.sh` is executable
    - `bash -n tools/ci/setup_godot.sh` exits 0 (no syntax errors)
    - `tools/ci/setup_godot.sh` contains `GITHUB_PATH` and `GITHUB_ENV` (integração correta com o runner do GH Actions)
    - `tools/ci/setup_godot.sh` contains `export_templates`
  </acceptance_criteria>
  <done>setup_godot.sh baixa e cacheia o editor + export templates da versão pinada, expondo `godot` no PATH do runner.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Estender validate.yml e client-ci.yml</name>
  <files>.github/workflows/validate.yml, .github/workflows/client-ci.yml, README.md</files>
  <read_first>
    - .github/workflows/validate.yml (versão atual, só roda validate-repo.sh)
    - .github/workflows/client-ci.yml (versão atual, já tem o guard "Projeto Godot existe?")
  </read_first>
  <action>
    1. Em `.github/workflows/validate.yml`, adicione um passo de lint de docs depois do passo
       `validate-repo` existente, dentro do mesmo job:
    ```yaml
      - name: lint de docs
        run: ./tools/ci/lint_docs.sh
    ```

    2. Em `.github/workflows/client-ci.yml`:
       a. Adicione cache do runner por versão antes do passo "Instalar Godot":
       ```yaml
       - name: Cache Godot
         if: steps.guard.outputs.ready == 'true'
         uses: actions/cache@v4
         with:
           path: ~/.cache/godot-ci
           key: godot-${{ hashFiles('.godot-version') }}
       ```
       b. Confirme que o passo "Instalar Godot" continua chamando
          `./tools/ci/setup_godot.sh "$(cat .godot-version)"` (já está assim — sem mudança
          necessária além de garantir que o `if: steps.guard.outputs.ready == 'true'` segue
          presente).
       c. **Remova** o passo final "Amostra de gameplay (20 partidas)" — `tools/dev/simulate.sh`
          só existe a partir de GSD 03; mantê-lo quebraria o CI desta fase por um script
          inexistente que não é responsabilidade desta fase criar. Adicione um comentário no
          lugar: `# tools/dev/simulate.sh chega em GSD 03 / TERR-014 — este passo volta aqui
          quando ele existir.`

    3. Em `README.md`, adicione badges de CI logo abaixo do título (se o README já tiver uma
       seção de badges, adicione ali; senão, logo após o bloco de tecnologias no topo):
    ```markdown
    ![validate](https://github.com/ricardosierra/volta/actions/workflows/validate.yml/badge.svg)
    ![client-ci](https://github.com/ricardosierra/volta/actions/workflows/client-ci.yml/badge.svg)
    ```
       (O repositório ainda não tem remote `git` configurado — os badges apontam para o slug
       esperado `ricardosierra/volta`, coerente com `ANDROID_PACKAGE_NAME=com.ricardosierra.volta`
       em `.env.example`; eles só vão "acender" quando o repo for publicado no GitHub e o CI
       rodar pelo menos uma vez. Documente isso no SUMMARY, não invente que já rodou.)

    4. Registre explicitamente no SUMMARY, como pendência (não como bloqueio desta fase): o
       "gate de merge com os dois workflows obrigatórios" (branch protection) depende de o
       repositório existir no GitHub primeiro (`git remote -v` está vazio agora). Quando o
       usuário decidir publicar o repositório, rodar:
       `gh repo create ricardosierra/volta --private --source=. --push` e então configurar
       branch protection em `develop`/`master` exigindo os dois workflows.
  </action>
  <verify>
    <automated>grep -c 'lint_docs.sh' .github/workflows/validate.yml; grep -c 'actions/cache@v4' .github/workflows/client-ci.yml; grep -c 'simulate.sh' .github/workflows/client-ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/validate.yml` contains `lint_docs.sh`
    - `.github/workflows/client-ci.yml` contains `actions/cache@v4`
    - `.github/workflows/client-ci.yml` does NOT contain an active (uncommented) `./tools/dev/simulate.sh` step
    - `README.md` contains `workflows/validate.yml/badge.svg`
    - `README.md` contains `workflows/client-ci.yml/badge.svg`
  </acceptance_criteria>
  <done>validate.yml cobre lint de docs; client-ci.yml cacheia o Godot e não referencia scripts que ainda não existem; README com badges; pendência de branch protection documentada no SUMMARY.</done>
</task>

</tasks>

<verification>
`bash -n tools/ci/setup_godot.sh` sem erro de sintaxe. `.github/workflows/validate.yml` e
`client-ci.yml` continuam sendo YAML válido (`python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))"`
para cada um, ou equivalente). Nenhum workflow referencia um script inexistente nesta fase.
</verification>

<success_criteria>
REPO-011 satisfeito: os dois workflows rodam sozinhos em todo push, com cache, e sem depender
de um script que só existe em fase futura. O gate de merge real (branch protection) fica
documentado como próximo passo humano quando o repo for publicado — não fabricado como já
feito.
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-09-SUMMARY.md`.
</output>
