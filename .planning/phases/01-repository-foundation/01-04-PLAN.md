---
phase: 01-repository-foundation
plan: 4
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - packages/shared/config/balance/runner.tres
  - packages/shared/config/balance/territory.tres
  - packages/shared/config/balance/backwash.tres
  - packages/shared/config/balance/score.tres
  - packages/shared/config/balance/surge.tres
  - packages/shared/config/balance/camera.tres
  - apps/mobile/src/core/config/runner_balance.gd
  - apps/mobile/src/core/config/territory_balance.gd
  - apps/mobile/src/core/config/backwash_balance.gd
  - apps/mobile/src/core/config/score_balance.gd
  - apps/mobile/src/core/config/surge_balance.gd
  - apps/mobile/src/core/config/camera_balance.gd
  - apps/mobile/src/core/config/config_service.gd
  - apps/mobile/src/core/config/config_validator.gd
  - apps/mobile/resources/config/balance/runner.tres
  - apps/mobile/resources/config/balance/territory.tres
  - apps/mobile/resources/config/balance/backwash.tres
  - apps/mobile/resources/config/balance/score.tres
  - apps/mobile/resources/config/balance/surge.tres
  - apps/mobile/resources/config/balance/camera.tres
  - tools/dev/sync_config.sh
  - apps/mobile/tests/unit/test_config_service.gd
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "ConfigService carrega os 6 .tres de balance e expõe valores tipados batendo com docs/design/balance.md, valor a valor"
    - "Um valor fora da faixa declarada é detectado e rejeitado, não aceito silenciosamente"
    - "Um campo deixado no default de tipo (0/0.0) onde isso é fisicamente inválido é detectado como campo ausente"
    - "packages/shared/config e apps/mobile/resources/config nunca divergem sem o CI perceber"
  artifacts:
    - path: "apps/mobile/src/core/config/config_service.gd"
      provides: "ConfigService.runner()/.territory()/.backwash()/.score()/.surge()/.camera() tipados e cacheados"
    - path: "apps/mobile/src/core/config/config_validator.gd"
      provides: "validação de faixa via introspecção de @export_range"
    - path: "packages/shared/config/balance/runner.tres"
      provides: "fonte única dos 9 parâmetros de Runner de docs/design/balance.md"
    - path: "tools/dev/sync_config.sh"
      provides: "sincronização packages/shared/config → apps/mobile/resources/config"
  key_links:
    - from: "apps/mobile/src/core/config/config_service.gd"
      to: "apps/mobile/resources/config/balance/runner.tres"
      via: "ResourceLoader.load"
      pattern: "resources/config/balance"
    - from: "apps/mobile/src/core/config/config_validator.gd"
      to: "PROPERTY_HINT_RANGE"
      via: "get_property_list() introspection"
      pattern: "PROPERTY_HINT_RANGE"
---

<objective>
Implementar `ConfigService` e os 6 `Resource` tipados de balance (REPO-005) — a única forma
pela qual um número de gameplay entra no jogo. Carrega, valida faixa e coerência, e falha alto
em debug / cai no embutido em release.

Purpose: nenhuma fase seguinte pode escrever `speed = 220.0` num `.gd` — precisa ler de
`ConfigService.runner().base_speed`. Esta é a peça que torna isso possível e obrigatório.

Output: `packages/shared/config/balance/*.tres` com os valores exatos de
`docs/design/balance.md`; `ConfigService` tipado e validado; `sync_config.sh` mantendo
`apps/mobile/resources/config` idêntico à fonte única.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/architecture/configuration.md
@docs/design/balance.md
@.planning/phases/01-repository-foundation/01-CONTEXT.md

<interfaces>
De `apps/mobile/src/core/log/log.gd` (Plano 01-02): `Log.error(Log.Category.ERROR, key, data)`
— use para reportar config inválida em release. `Log.Category` já tem os 10 valores
documentados; use `ERROR` para falha de validação.

Este plano NÃO depende de `Bootstrap` estar wireado (ver Plano 01-02): `ConfigService` é
testado instanciando-o diretamente (`ConfigService.new()`), não através do boot completo. A
integração real no `Bootstrap` de produção (registrar `"config"` na lista de passos) acontece
quando `apps/mobile/scenes/main.tscn` existir (Plano 01-10) — até lá, o serviço é standalone
e testável isoladamente, exatamente como `TESTS.md` pede (testes unitários diretos, não de
integração).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Os 6 Resources de balance — contratos tipados + .tres com os valores de balance.md</name>
  <files>
    apps/mobile/src/core/config/runner_balance.gd, territory_balance.gd, backwash_balance.gd,
    score_balance.gd, surge_balance.gd, camera_balance.gd,
    packages/shared/config/balance/{runner,territory,backwash,score,surge,camera}.tres
  </files>
  <read_first>
    - docs/design/balance.md (TODOS os valores — copie exatamente, sem arredondar)
    - docs/architecture/configuration.md §1-2
  </read_first>
  <action>
    Crie 6 classes `Resource` tipadas, uma por arquivo, cada campo com `@export_range(min, max)`
    (o range vira a faixa de validação — ver Task 2). Use exatamente estes campos e valores
    (default = valor 🎯 de `docs/design/balance.md`; ranges abaixo são a faixa de validação
    escolhida nesta implementação — mantenha o valor 🎯 sempre dentro do range):

    **`runner_balance.gd`** (`class_name RunnerBalance extends Resource`):
    | Campo | Range | Default |
    |---|---|---|
    | `base_speed: float` | 50.0–600.0 | 220.0 |
    | `turn_rate: float` | 90.0–1080.0 | 540.0 |
    | `collision_radius: float` | 4.0–32.0 | 10.0 |
    | `arc_visual_width: float` | 2.0–16.0 | 6.0 |
    | `spawn_invuln: float` | 0.0–10.0 | 2.0 |
    | `respawn_delay: float` | 0.0–10.0 | 1.5 |
    | `respawn_delay_time_attack: float` | 0.0–10.0 | 3.0 |
    | `arc_max_cells: int` | 100–5000 | 1200 |
    | `overload_decay: float` | 1.0–50.0 | 8.0 |

    **`territory_balance.gd`** (`class_name TerritoryBalance extends Resource`):
    | Campo | Range | Default |
    |---|---|---|
    | `cell_size: float` | 4.0–64.0 | 16.0 |
    | `grid_default_width: int` | 32–512 | 128 |
    | `grid_default_height: int` | 32–512 | 128 |
    | `grid_small_width: int` | 32–512 | 96 |
    | `grid_small_height: int` | 32–512 | 96 |
    | `grid_large_width: int` | 32–512 | 160 |
    | `grid_large_height: int` | 32–512 | 160 |
    | `spawn_claim_cells: int` | 1–20 | 5 |
    | `spawn_min_distance: int` | 1–200 | 28 |
    | `border_thickness: int` | 1–8 | 1 |

    **`backwash_balance.gd`** (`class_name BackwashBalance extends Resource`):
    | Campo | Range | Default |
    |---|---|---|
    | `speed_penalty: float` | -0.9–0.0 | -0.40 |
    | `penalty_duration: float` | 0.0–10.0 | 1.2 |
    | `reset_surge_on_backwash: bool` | — | true |
    | `tangent_deflection: bool` | — | true |

    **`score_balance.gd`** (`class_name ScoreBalance extends Resource`):
    | Campo | Range | Default |
    |---|---|---|
    | `cell_value: int` | 1–100 | 4 |
    | `stolen_cell_mult: float` | 1.0–5.0 | 1.6 |
    | `risk_divisor: float` | 10.0–1000.0 | 150.0 |
    | `risk_cap: float` | 1.0–5.0 | 1.5 |
    | `break_base: int` | 0–5000 | 600 |
    | `break_streak_step: int` | 0–5000 | 300 |
    | `break_streak_cap: int` | 1–20 | 5 |
    | `survival_per_second: int` | 0–100 | 5 |
    | `territory_weight: int` | 0–50000 | 8000 |
    | `largest_seal_weight: int` | 0–50000 | 4000 |
    | `placement_bonus_1st: int` | 0–10000 | 2500 |
    | `placement_bonus_2nd: int` | 0–10000 | 1200 |
    | `placement_bonus_3rd: int` | 0–10000 | 600 |
    | `placement_bonus_other: int` | 0–10000 | 0 |
    | `victory_mult: float` | 1.0–3.0 | 1.25 |
    | `push_mult: float` | 1.0–3.0 | 1.25 |

    **`surge_balance.gd`** (`class_name SurgeBalance extends Resource`):
    | Campo | Range | Default |
    |---|---|---|
    | `surge_step: float` | 0.01–1.0 | 0.15 |
    | `surge_max_level: int` | 1–20 | 6 |
    | `seal_chain_window: float` | 1.0–30.0 | 8.0 |
    | `break_chain_window: float` | 1.0–30.0 | 10.0 |
    | `surge_decay_interval: float` | 1.0–30.0 | 8.0 |

    **`camera_balance.gd`** (`class_name CameraBalance extends Resource`):
    | Campo | Range | Default |
    |---|---|---|
    | `zoom_base: float` | 0.5–2.0 | 1.0 |
    | `zoom_min: float` | 0.3–2.0 | 0.75 |
    | `zoom_max: float` | 0.3–3.0 | 1.35 |
    | `follow_smoothing: float` | 1.0–20.0 | 8.0 |
    | `lookahead: float` | 0.0–300.0 | 90.0 |
    | `punch_seal_amplitude: float` | 0.0–1.0 | 0.03 |
    | `punch_seal_duration: float` | 0.0–1.0 | 0.12 |
    | `punch_break_amplitude: float` | 0.0–1.0 | 0.06 |
    | `punch_break_duration: float` | 0.0–1.0 | 0.18 |
    | `shake_max: float` | 0.0–20.0 | 4.0 |

    Exemplo de padrão para cada campo `float`/`int` (não use em campos `bool`):
    ```gdscript
    @export_range(50.0, 600.0) var base_speed: float = 220.0
    ```
    Campos `bool` são apenas `@export var reset_surge_on_backwash: bool = true` (sem faixa —
    o validador da Task 2 pula bools).

    Salve cada `.tres` em `packages/shared/config/balance/` com os defaults acima (que já
    SÃO os valores de `docs/design/balance.md` — não há necessidade de sobrescrever nada
    depois de instanciar com `.new()` e salvar via `ResourceSaver.save()`, ou edite o `.tres`
    devidamente formatado à mão).
  </action>
  <verify>
    <automated>GODOT_BIN=$(command -v godot || echo "/Applications/Godot_CLI.app/Contents/MacOS/Godot"); "$GODOT_BIN" --headless --path apps/mobile --check-only; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/config/balance/runner.tres` contains `base_speed = 220.0` (or `220`)
    - `packages/shared/config/balance/territory.tres` contains `cell_size = 16.0`
    - `packages/shared/config/balance/score.tres` contains `cell_value = 4`
    - `packages/shared/config/balance/surge.tres` contains `surge_max_level = 6`
    - `packages/shared/config/balance/camera.tres` contains `follow_smoothing = 8.0`
    - `packages/shared/config/balance/backwash.tres` contains a value approximately `-0.4` for `speed_penalty`
    - Each of the 6 `*_balance.gd` files contains `class_name` and at least one `@export_range(`
    - No `.gd` file outside `apps/mobile/src/core/config/` created in this task contains a gameplay literal
  </acceptance_criteria>
  <done>6 Resources tipados criados; 6 .tres em packages/shared/config/balance/ com os valores exatos de balance.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfigService + ConfigValidator + sync_config.sh</name>
  <files>
    apps/mobile/src/core/config/config_service.gd, apps/mobile/src/core/config/config_validator.gd,
    apps/mobile/resources/config/balance/*.tres, tools/dev/sync_config.sh,
    apps/mobile/tests/unit/test_config_service.gd
  </files>
  <read_first>
    - docs/architecture/configuration.md §2-3 (acesso tipado, validação obrigatória, falha alto em debug / embutido em release)
    - apps/mobile/src/core/config/runner_balance.gd (criado na Task 1 — confirme os nomes de campo exatos antes de escrever o validador)
  </read_first>
  <behavior>
    - `ConfigValidator.validate(resource)` itera `resource.get_property_list()`, filtra
      propriedades com `usage & PROPERTY_USAGE_SCRIPT_VARIABLE` e `hint == PROPERTY_HINT_RANGE`,
      faz `hint_string.split(",")` para obter `min,max` (primeiros dois tokens, convertidos
      para `float`), e falha se `resource.get(property.name)` estiver fora de `[min, max]`.
    - **Teste "válida"**: carregar `runner.tres` como está (Task 1) passa na validação.
    - **Teste "fora de faixa"**: um `RunnerBalance.new()` com `base_speed = 99999.0` (fora do
      range 50–600) falha na validação, e a falha identifica o campo `"base_speed"`.
    - **Teste "campo ausente"**: um `RunnerBalance.new()` sem setar `turn_rate` explicitamente
      fica no default de classe (`540.0` — que É válido). Para simular um campo genuinamente
      ausente/zerado, o teste cria a instância e **explicitamente** zera um campo cujo range
      mínimo é > 0 (ex.: `resource.arc_max_cells = 0`, fora do range 100–5000) — isso modela
      "campo que deveria ter sido preenchido mas ficou no valor inválido de tipo" e é
      detectado pelo **mesmo mecanismo de faixa**, sem precisar de um segundo código de
      validação. Documente essa equivalência no teste com um comentário.
  </behavior>
  <action>
    1. `apps/mobile/src/core/config/config_validator.gd`:
    ```gdscript
    class_name ConfigValidator
    extends RefCounted

    ## Retorna um Array de Strings com os nomes dos campos inválidos (vazio = válido).
    static func validate(resource: Resource) -> Array[String]:
        var invalid: Array[String] = []
        for prop in resource.get_property_list():
            if not (prop.get("usage", 0) & PROPERTY_USAGE_SCRIPT_VARIABLE):
                continue
            if prop.get("hint", PROPERTY_HINT_NONE) != PROPERTY_HINT_RANGE:
                continue
            var parts := String(prop["hint_string"]).split(",")
            if parts.size() < 2:
                continue
            var min_v := float(parts[0])
            var max_v := float(parts[1])
            var value = resource.get(prop["name"])
            if typeof(value) != TYPE_FLOAT and typeof(value) != TYPE_INT:
                continue
            if float(value) < min_v or float(value) > max_v:
                invalid.append(String(prop["name"]))
        return invalid
    ```

    2. `apps/mobile/src/core/config/config_service.gd`:
    ```gdscript
    class_name ConfigService
    extends RefCounted

    const BASE_PATH: String = "res://resources/config/balance/"

    var _runner: RunnerBalance
    var _territory: TerritoryBalance
    var _backwash: BackwashBalance
    var _score: ScoreBalance
    var _surge: SurgeBalance
    var _camera: CameraBalance
    var _load_error: String = ""

    func load_all() -> bool:
        _runner = _load_and_validate("runner.tres", RunnerBalance.new()) as RunnerBalance
        _territory = _load_and_validate("territory.tres", TerritoryBalance.new()) as TerritoryBalance
        _backwash = _load_and_validate("backwash.tres", BackwashBalance.new()) as BackwashBalance
        _score = _load_and_validate("score.tres", ScoreBalance.new()) as ScoreBalance
        _surge = _load_and_validate("surge.tres", SurgeBalance.new()) as SurgeBalance
        _camera = _load_and_validate("camera.tres", CameraBalance.new()) as CameraBalance
        return _load_error == ""

    func runner() -> RunnerBalance: return _runner
    func territory() -> TerritoryBalance: return _territory
    func backwash() -> BackwashBalance: return _backwash
    func score() -> ScoreBalance: return _score
    func surge() -> SurgeBalance: return _surge
    func camera() -> CameraBalance: return _camera
    func get_load_error() -> String: return _load_error

    func _load_and_validate(file_name: String, fallback: Resource) -> Resource:
        var loaded: Resource = ResourceLoader.load(BASE_PATH + file_name)
        var candidate: Resource = loaded if loaded != null else fallback
        var invalid := ConfigValidator.validate(candidate)
        if not invalid.is_empty():
            var msg := "config inválida em %s: campos fora de faixa: %s" % [file_name, invalid]
            _load_error += msg + "; "
            if Log:
                Log.error(Log.Category.ERROR, "config_invalid", {"file": file_name, "fields": invalid})
            if Build.is_debug():
                push_error(msg)  # falha alto em debug
                return fallback
            return fallback  # cai no embutido em release
        return candidate
    ```
       Nota: "falha alto em debug" aqui significa erro **visível e loud** (`push_error` +
       `Log.error`), não `assert()`/crash — travar o editor a cada config levemente errada
       tornaria o dia a dia inviável; o objetivo é que a falha seja impossível de ignorar,
       não que o processo morra.

    3. Copie os 6 `.tres` da Task 1 para `apps/mobile/resources/config/balance/` (conteúdo
       idêntico — esta é a cópia importada que o CI valida estar sincronizada com a fonte em
       `packages/shared/config`).

    4. `tools/dev/sync_config.sh`:
    ```bash
    #!/usr/bin/env bash
    # Sincroniza packages/shared/config -> apps/mobile/resources/config (fonte única -> projeto).
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1
    mkdir -p apps/mobile/resources/config
    rsync -a --delete packages/shared/config/ apps/mobile/resources/config/ 2>/dev/null \
      || cp -R packages/shared/config/. apps/mobile/resources/config/
    echo "OK: packages/shared/config sincronizado para apps/mobile/resources/config"
    ```
       Torne executável (`chmod +x`).

    5. `apps/mobile/tests/unit/test_config_service.gd` (GUT):
       - `test_valid_config_loads`: `var svc := ConfigService.new(); assert_true(svc.load_all()); assert_eq(svc.runner().base_speed, 220.0)`
       - `test_out_of_range_value_detected`: cria `RunnerBalance.new()`, seta
         `base_speed = 99999.0`, chama `ConfigValidator.validate(resource)` diretamente,
         assevera que o array retornado contém `"base_speed"`.
       - `test_missing_field_detected`: cria `RunnerBalance.new()`, seta
         `arc_max_cells = 0` (fora do range 100–5000, modelando "campo não preenchido" — ver
         `<behavior>` acima), assevera que `ConfigValidator.validate(resource)` contém
         `"arc_max_cells"`.
       - `test_all_six_balances_load_without_error`: `assert_true(svc.load_all())`;
         assevera `svc.get_load_error() == ""`.
  </action>
  <verify>
    <automated>./tools/ci/test-client.sh</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/core/config/config_validator.gd` contains `PROPERTY_HINT_RANGE`
    - `apps/mobile/src/core/config/config_service.gd` contains `func runner() -> RunnerBalance`
    - `apps/mobile/src/core/config/config_service.gd` contains `func load_all() -> bool`
    - `tools/dev/sync_config.sh` is executable
    - `apps/mobile/resources/config/balance/runner.tres` exists and is byte-identical to `packages/shared/config/balance/runner.tres` (`diff` exits 0)
    - `apps/mobile/tests/unit/test_config_service.gd` contains 4 `func test_` definitions matching the four scenarios above
    - `./tools/ci/test-client.sh` exits 0 with these tests included
  </acceptance_criteria>
  <done>ConfigService carrega e valida os 6 balances; 3+ testes cobrindo válida/fora-de-faixa/campo-ausente passam; sync_config.sh mantém as duas cópias idênticas.</done>
</task>

</tasks>

<verification>
`./tools/ci/test-client.sh` inclui e passa os testes de `test_config_service.gd`. Comparação
manual (`diff packages/shared/config/balance/runner.tres apps/mobile/resources/config/balance/runner.tres`)
não mostra diferenças. Nenhum valor de `docs/design/balance.md` foi arredondado.
</verification>

<success_criteria>
`ConfigService.runner().base_speed` etc. são a única forma legítima de obter esses números —
qualquer `.gd` de gameplay futuro que escrever `220.0` literalmente será pego por
`validate-repo.sh` (Plano 01-07/09) em fases seguintes.
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-04-SUMMARY.md`.
</output>
