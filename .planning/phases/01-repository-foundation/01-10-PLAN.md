---
phase: 01-repository-foundation
plan: 10
type: execute
wave: 6
depends_on: ["01-09"]
files_modified:
  - apps/mobile/scenes/main.tscn
  - apps/mobile/src/core/dev_overlay.gd
  - apps/mobile/src/core/bootstrap.gd
  - tools/ci/export_presets.template.cfg
  - tools/ci/make_export_presets.sh
  - tools/ci/build_android.sh
  - apps/mobile/.gitignore
  - .gitignore
  - docs/performance/device-results.md
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "Existe uma cena principal que mostra nome, versão, FPS e um tier de dispositivo estimado, atrás de Build.is_debug()"
    - "Um APK de debug é gerado localmente por um único comando (tools/ci/build_android.sh debug), sem passo manual no editor"
    - "docs/performance/device-results.md existe com a estrutura pronta para a medição em aparelho real (Plano 01-11)"
  artifacts:
    - path: "apps/mobile/scenes/main.tscn"
      provides: "cena principal mínima (PLACEHOLDER-ART-006), transicionada por Bootstrap"
    - path: "tools/ci/build_android.sh"
      provides: "export de APK de debug via CLI do Godot"
    - path: "docs/performance/device-results.md"
      provides: "tabela de registro de medições em dispositivo, pronta para o Plano 01-11 preencher"
  key_links:
    - from: "apps/mobile/src/core/bootstrap.gd"
      to: "apps/mobile/scenes/main.tscn"
      via: "next_scene_path (uma linha de dado em bootstrap.gd, autoload)"
      pattern: "next_scene_path"
    - from: "apps/mobile/src/core/dev_overlay.gd"
      to: "apps/mobile/src/core/build.gd"
      via: "Build.is_debug() / Build.version()"
      pattern: "Build\\.(is_debug|version)"
---

<objective>
Criar a cena principal mínima que prova o pipeline inteiro (REPO-012, parte automatizável) e
gerar um APK de debug localmente via CLI — sem depender do editor gráfico do Godot nem de um
passo manual.

Purpose: é aqui, na Fase 1, que problemas de export aparecem — não na Fase 21. Este plano
automatiza tudo que NÃO exige um aparelho físico; a confirmação em hardware real fica isolada
no Plano 01-11 (checkpoint).

Output: `main.tscn` com fundo, versão, FPS e tier de dispositivo (overlay atrás de
`Build.is_debug()`); `build_android.sh debug` produzindo um `.apk` em `dist/android/`;
`docs/performance/device-results.md` com a tabela pronta.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/mobile/android.md
@docs/mobile/device-matrix.md
@docs/performance/performance-budget.md
@docs/architecture/debug-tools.md
@.gsd/phases/01-repository-foundation/ACCEPTANCE.md

<environment_facts>
Verificado nesta máquina em 2026-08-24 (documente se mudou quando este plano rodar):
- Godot 4.3.stable: `/Applications/Godot_CLI.app/Contents/MacOS/Godot` (não em PATH).
- Export templates 4.3.stable: instalados em `~/Library/Application Support/Godot/export_templates/4.3.stable`.
- Android SDK: `~/Library/Android/sdk` (platforms 34–37, build-tools 33–36 instalados), `adb` em `~/Library/Android/sdk/platform-tools/adb`.
- `java`: OpenJDK 25 (Temurin) em `/usr/bin/java`.
- **Nenhum dispositivo Android conectado no momento deste planejamento** (`adb devices` vazio)
  — normal, o dispositivo só é necessário no Plano 01-11 (checkpoint humano).
- `ANDROID_PACKAGE_NAME=com.ricardosierra.volta` (`.env.example`).

Resolva o binário do Godot exatamente como nos planos anteriores desta fase (`GODOT_BIN` →
`command -v godot` → caminho conhecido do `.app`).
</environment_facts>

<interfaces>
De `apps/mobile/src/core/bootstrap.gd` (Plano 01-02, autoload `Bootstrap`, sem `class_name`):
`var next_scene_path: String = ""` — este plano troca essa única linha para
`var next_scene_path: String = "res://scenes/main.tscn"` (edição de dado; nenhuma lógica nova).
`boot.tscn` (Plano 01-01, nó `Boot` sem script) continua sendo a cena principal; `Bootstrap`
faz `change_scene_to_file.call_deferred(next_scene_path)` ao fim do `boot()`.

De `apps/mobile/src/core/build.gd` (Plano 01-01): `Build.is_debug()`, `Build.version()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: main.tscn + DevOverlay</name>
  <files>apps/mobile/scenes/main.tscn, apps/mobile/src/core/dev_overlay.gd, apps/mobile/src/core/bootstrap.gd</files>
  <read_first>
    - docs/architecture/debug-tools.md (regra: overlay atrás de Build.is_debug())
    - apps/mobile/src/core/build.gd (Build.version()/is_debug())
    - apps/mobile/src/core/bootstrap.gd (Plano 01-02 — a linha `var next_scene_path: String = ""` que este plano edita)
  </read_first>
  <action>
    1. `apps/mobile/src/core/dev_overlay.gd`:
    ```gdscript
    class_name DevOverlay
    extends CanvasLayer

    var _label: Label

    func _ready() -> void:
        if not Build.is_debug():
            queue_free()
            return
        _label = Label.new()
        _label.position = Vector2(24, 48)
        add_child(_label)

    func _process(_delta: float) -> void:
        if _label == null:
            return
        var fps := Engine.get_frames_per_second()
        var mem_mb := OS.get_static_memory_usage() / 1048576.0
        _label.text = "VOLTA v%s\nFPS: %d\nMem: %.1f MB\nTier: %s" % [Build.version(), fps, mem_mb, _detect_tier()]

    func _detect_tier() -> String:
        # Heurística simplificada por contagem de núcleos, só para esta cena de prova de
        # pipeline. A detecção completa (RAM + renderer + micro-benchmark de 1s) entra em
        # GSD 19/20 junto dos presets de qualidade (packages/shared/config/quality/).
        var cores := OS.get_processor_count()
        if cores >= 8:
            return "High"
        if cores >= 5:
            return "Mid"
        return "Low"
    ```

    2. `apps/mobile/scenes/main.tscn`: um `Node2D` raiz (`Main`) com filhos:
       - `ColorRect` cobrindo a tela inteira, `color = Color(0.043, 0.055, 0.078, 1.0)`
         (aproximação provisória de `bg.deep` — o token real não tem valor definido antes de
         GSD 08). Anote no `.tscn` (ou num comentário no `dev_overlay.gd` próximo, já que
         `.tscn` não aceita comentário de linha facilmente): `# PLACEHOLDER-ART-006 — cor de
         fundo provisória, Replacement: GSD 08`.
       - `Label` centralizado com texto `"VOLTA"` (nome do app) — pode usar a fonte default do
         Godot, sem estilização (design system é GSD 07).
       - `DevOverlay` (nó `CanvasLayer` com o script acima) como filho.

    3. Em `apps/mobile/src/core/bootstrap.gd`, troque a linha
       `var next_scene_path: String = ""` por
       `var next_scene_path: String = "res://scenes/main.tscn"` — só isso; nenhuma outra
       mudança no autoload. `boot.tscn` não muda.
  </action>
  <verify>
    <automated>./tools/ci/check-project.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/scenes/main.tscn` exists and references `dev_overlay.gd`
    - `apps/mobile/src/core/dev_overlay.gd` contains `PLACEHOLDER-ART-006` reference in a comment OR the .tscn file does
    - `apps/mobile/src/core/dev_overlay.gd` contains `Build.is_debug()`
    - `apps/mobile/src/core/dev_overlay.gd` contains `Build.version()`
    - `grep -c 'var next_scene_path: String = "res://scenes/main.tscn"' apps/mobile/src/core/bootstrap.gd` returns 1
    - `./tools/ci/check-project.sh` exits 0
  </acceptance_criteria>
  <done>main.tscn existe com fundo provisório, nome, versão, FPS e tier estimado atrás de Build.is_debug(); boot.tscn transiciona para ela.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Export presets + build_android.sh debug</name>
  <files>
    tools/ci/export_presets.template.cfg, tools/ci/make_export_presets.sh,
    tools/ci/build_android.sh, apps/mobile/.gitignore, .gitignore
  </files>
  <read_first>
    - docs/mobile/android.md (minSdk 24, targetSdk 34, arm64-v8a + armeabi-v7a, portrait, package com.ricardosierra.volta)
    - .env.example (ANDROID_PACKAGE_NAME, ANDROID_SDK_ROOT)
  </read_first>
  <action>
    1. `tools/ci/export_presets.template.cfg` (template versionado; o `.cfg` real gerado por
       `make_export_presets.sh` fica fora do controle de versão, como já garante
       `export_presets.cfg` no `.gitignore` raiz):
    ```ini
    [preset.0]

    name="Android"
    platform="Android"
    runnable=true
    export_path="../../dist/android/volta.apk"
    encryption_include_filters=""
    encryption_exclude_filters=""

    [preset.0.options]

    custom_template/debug=""
    custom_template/release=""
    gradle_build/use_gradle_build=false
    version/code=1
    version/name="0.1.0"
    package/unique_name="__ANDROID_PACKAGE_NAME__"
    package/name="VOLTA"
    architectures/arm64-v8a=true
    architectures/armeabi-v7a=true
    screen/orientation="portrait"
    permissions/internet=false
    ```

    2. `tools/ci/make_export_presets.sh`:
    ```bash
    #!/usr/bin/env bash
    # Gera apps/mobile/export_presets.cfg a partir do template + variáveis de ambiente.
    # O .cfg gerado NUNCA é versionado (ver .gitignore).
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1

    PACKAGE_NAME="${ANDROID_PACKAGE_NAME:-com.ricardosierra.volta}"
    sed "s|__ANDROID_PACKAGE_NAME__|$PACKAGE_NAME|g" tools/ci/export_presets.template.cfg > apps/mobile/export_presets.cfg
    echo "OK: apps/mobile/export_presets.cfg gerado (package: $PACKAGE_NAME)"
    ```
       Torne executável.

    3. `tools/ci/build_android.sh`:
    ```bash
    #!/usr/bin/env bash
    # Exporta o APK de debug (ou falha explicando o que falta) via CLI do Godot.
    set -uo pipefail
    cd "$(dirname "$0")/../.." || exit 1

    MODE="${1:-debug}"
    GODOT_BIN="${GODOT_BIN:-}"
    if [ -z "$GODOT_BIN" ]; then
      if command -v godot >/dev/null 2>&1; then GODOT_BIN=godot
      elif [ -x "/Applications/Godot_CLI.app/Contents/MacOS/Godot" ]; then GODOT_BIN="/Applications/Godot_CLI.app/Contents/MacOS/Godot"
      else echo "ERRO: binário do Godot não encontrado. Defina GODOT_BIN." >&2; exit 1
      fi
    fi

    ./tools/ci/make_export_presets.sh
    mkdir -p dist/android

    case "$MODE" in
      debug)
        "$GODOT_BIN" --headless --path apps/mobile --export-debug "Android" ../../dist/android/volta-debug.apk
        code=$?
        ;;
      release)
        echo "build de release é GSD 21 (ANDR-001..003) — não implementado nesta fase." >&2
        exit 1
        ;;
      *)
        echo "modo desconhecido: $MODE (use 'debug')" >&2
        exit 1
        ;;
    esac

    if [ $code -eq 0 ] && [ -f dist/android/volta-debug.apk ]; then
      echo "✅ APK gerado em dist/android/volta-debug.apk"
    else
      echo "❌ export falhou (exit $code). Verifique export templates 4.3.stable e Android SDK/JDK (tools/dev/doctor.sh)." >&2
      exit 1
    fi
    ```
       Torne executável.

    4. Adicione `dist/` ao `.gitignore` raiz (o `.apk` gerado não é versionado).

    5. Rode `./tools/ci/build_android.sh debug` de verdade nesta tarefa. Este ambiente TEM
       Godot 4.3, export templates 4.3.stable, Android SDK (platforms 34-37, build-tools
       33-36) e JDK 25 — o export deve funcionar de ponta a ponta sem hardware conectado (o
       APK só precisa ser **gerado**, não instalado, para esta tarefa passar). Se o export
       falhar por qualquer motivo (ex.: `gradle_build/use_gradle_build` incompatível, versão
       de build-tools, etc.), **não invente sucesso**: capture a mensagem de erro real, ajuste
       o preset/script até funcionar, e se depois de tentativas razoáveis ainda não
       funcionar, documente o erro exato e o estado no SUMMARY como pendência explícita —
       não afirme que o APK existe se ele não existe.
  </action>
  <verify>
    <automated>./tools/ci/build_android.sh debug; echo "exit=$?"; ls -la dist/android/volta-debug.apk 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `tools/ci/build_android.sh` and `tools/ci/make_export_presets.sh` are executable
    - `tools/ci/export_presets.template.cfg` contains `screen/orientation="portrait"`
    - `tools/ci/export_presets.template.cfg` contains `architectures/arm64-v8a=true`
    - `.gitignore` contains `dist/`
    - Either `dist/android/volta-debug.apk` exists after running `build_android.sh debug`, OR the SUMMARY explicitly documents the exact export failure and current blocked state (never silently absent)
  </acceptance_criteria>
  <done>build_android.sh debug produz um APK real em dist/android/volta-debug.apk nesta máquina, ou a falha real está documentada no SUMMARY sem fabricar sucesso.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: docs/performance/device-results.md — estrutura pronta para medição</name>
  <files>docs/performance/device-results.md</files>
  <read_first>
    - docs/mobile/device-matrix.md (tiers, proporções, checklist por aparelho)
    - .gsd/phases/01-repository-foundation/ACCEPTANCE.md (A01-13: cold start < 1,5s; A01-14: 60 FPS)
  </read_first>
  <action>
    Crie `docs/performance/device-results.md`:
    ```markdown
    # Resultados por dispositivo

    Medições reais em aparelho físico, por versão do app. Ver
    [`../mobile/device-matrix.md`](../mobile/device-matrix.md) para os tiers e o checklist
    completo por aparelho.

    ## Phase 1 — prova de pipeline (cena mínima)

    | Campo | Valor |
    |---|---|
    | Data | _pendente — preenchido no checkpoint do Plano 01-11_ |
    | Aparelho | _pendente_ |
    | Tier | _pendente_ |
    | Android (versão) | _pendente_ |
    | Cold start (alvo < 1,5 s) | _pendente_ |
    | FPS estável (alvo 60) | _pendente_ |
    | Safe area respeitada | _pendente_ |
    | Versão exibida bate com project.godot (0.1.0) | _pendente_ |
    | Observações | _pendente_ |

    ## Histórico

    | Data | Versão do app | Fase | Resumo |
    |---|---|---|---|
    | — | — | — | tabela criada em GSD 01 / REPO-012, primeira medição real pendente |
    ```
  </action>
  <verify>
    <automated>test -f docs/performance/device-results.md && grep -c '## Phase 1' docs/performance/device-results.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/performance/device-results.md` exists
    - `docs/performance/device-results.md` contains `## Phase 1`
    - `docs/performance/device-results.md` contains a table row for `Cold start`
  </acceptance_criteria>
  <done>Tabela de device-results.md pronta para o Plano 01-11 preencher com números reais.</done>
</task>

</tasks>

<verification>
`./tools/ci/check-project.sh` exits 0 com `main.tscn`, `dev_overlay.gd` e a transição boot → main incluídos.
`dist/android/volta-debug.apk` existe (ou a falha real de export está documentada no SUMMARY).
`docs/performance/device-results.md` existe com a estrutura pronta.
</verification>

<success_criteria>
Tudo que não exige um aparelho físico conectado está automatizado e provado: cena mínima,
overlay de debug, geração de APK via CLI. O que falta é só a confirmação visual em hardware
real — isolada no Plano 01-11.
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-10-SUMMARY.md`.
</output>
