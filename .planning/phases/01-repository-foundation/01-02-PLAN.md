---
phase: 01-repository-foundation
plan: 2
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/mobile/src/core/log/log_category.gd
  - apps/mobile/src/core/log/log_sink.gd
  - apps/mobile/src/core/log/file_log_sink.gd
  - apps/mobile/src/core/log/log.gd
  - apps/mobile/src/core/service_registry.gd
  - apps/mobile/src/core/bootstrap.gd
  - apps/mobile/project.godot
  - docs/architecture/overview.md
autonomous: true
requirements: [FND-01, FND-04]
must_haves:
  truths:
    - "Uma mensagem de log com nível desabilitado não monta o dicionário de dados nem chama nenhum sink"
    - "O arquivo de log em user://logs/ gira em 5 arquivos de 2 MB sem crescer indefinidamente"
    - "O boot inicializa os serviços em ordem determinística e uma falha de serviço não essencial não derruba o boot"
    - "ServiceRegistry.resolve() de um serviço inexistente falha de forma legível e testável, nunca com null silencioso sem explicação"
  artifacts:
    - path: "apps/mobile/src/core/log/log.gd"
      provides: "Log.info/warn/error/debug + Log.enabled(cat, lvl) + Log.Category"
    - path: "apps/mobile/src/core/service_registry.gd"
      provides: "register()/resolve()/has() com erro legível"
    - path: "apps/mobile/src/core/bootstrap.gd"
      provides: "boot() determinístico sobre uma lista configurável de passos"
    - path: "apps/mobile/project.godot"
      provides: "[autoload] com exatamente dois singletons, nesta ordem: Log, Bootstrap"
  key_links:
    - from: "apps/mobile/project.godot"
      to: "apps/mobile/src/core/log/log.gd"
      via: "[autoload] Log=..."
      pattern: "Log=\"\\*res://src/core/log/log\\.gd\""
    - from: "apps/mobile/project.godot"
      to: "apps/mobile/src/core/bootstrap.gd"
      via: "[autoload] Bootstrap=..."
      pattern: "Bootstrap=\"\\*res://src/core/bootstrap\\.gd\""
    - from: "apps/mobile/src/core/bootstrap.gd"
      to: "apps/mobile/src/core/service_registry.gd"
      via: "registry.register(...)"
      pattern: "ServiceRegistry"
---

<objective>
Implementar `Log` (REPO-003) — logging estruturado por categoria e nível, sem custo quando
desligado — e `ServiceRegistry` + `Bootstrap` (REPO-004) — o mecanismo genérico e determinístico
que monta o grafo de serviços do jogo, sem singleton mágico.

Purpose: toda tarefa a partir daqui (Config, Save, EventBus) se registra neste mecanismo.
`Log` também é consumido por eles para reportar sucesso/falha de carga.

Output: `Log` (autoload) funcionando com rotação de arquivo; `Bootstrap` (o segundo e último
autoload) rodando `boot()` ao entrar na árvore, testável com serviços falsos.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repository-foundation/01-CONTEXT.md
@docs/architecture/logging.md
@docs/architecture/overview.md
@CLAUDE.md

<interfaces>
De `apps/mobile/src/core/build.gd` (Plano 01-01, já implementado):
```gdscript
class_name Build
extends RefCounted
static func is_debug() -> bool
static func version() -> String
static func commit() -> String
static func env() -> String
```
`Log` usa `Build.is_debug()` para decidir se o nível `DEBUG` está disponível.

De `apps/mobile/project.godot` (Plano 01-01, já criado): `run/main_scene="res://scenes/boot.tscn"`
já está definido e `apps/mobile/scenes/boot.tscn` já existe (nó `Boot`, sem script). **Não há**
seção `[autoload]` ainda — este plano a cria (Task 1 registra `Log`, Task 2 completa com
`Bootstrap`).

<autoload_rules>
**Decisão travada** (CONTEXT.md "Autoloads e injeção", `.gsd/TASKS.md` REPO-004 passo 4,
`.gsd/ACCEPTANCE.md` A01-15): exatamente dois autoloads, `Log` e `Bootstrap`, nesta ordem
(Godot instancia na ordem do `project.godot`; `Bootstrap` usa `Log` no `_ready()`):
```ini
[autoload]

Log="*res://src/core/log/log.gd"
Bootstrap="*res://src/core/bootstrap.gd"
```
**Fato verificado com o binário 4.3.stable desta máquina:** um script registrado como autoload
`X` **não pode** declarar `class_name X` — o parser rejeita com
`Parse Error: Class "X" hides an autoload singleton.` Portanto `log.gd` e `bootstrap.gd` NÃO
têm `class_name`; são acessados pelo nome do singleton (`Log.info(...)`, `Bootstrap.registry`).
É a única exceção à regra "class_name em PascalCase" de CLAUDE.md, imposta pela engine —
documente-a com um comentário na primeira linha de cada um dos dois arquivos.
Consequência para testes: `Bootstrap` dentro de um teste é a **instância** do singleton, então
instâncias novas vêm de `preload("res://src/core/bootstrap.gd").new()`, nunca de
`Bootstrap.new()`. Também verificado: num checkout limpo, o projeto só resolve os autoloads
depois de um `--import` — `tools/ci/check-project.sh` (01-01) e `test-client.sh` (01-03) já
fazem isso.
</autoload_rules>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Log — logging estruturado por categoria e nível</name>
  <files>
    apps/mobile/src/core/log/log_category.gd, apps/mobile/src/core/log/log_sink.gd,
    apps/mobile/src/core/log/file_log_sink.gd, apps/mobile/src/core/log/log.gd,
    apps/mobile/project.godot, apps/mobile/tests/unit/test_log.gd
  </files>
  <read_first>
    - docs/architecture/logging.md (categorias, níveis, API exata, regras de release)
    - apps/mobile/src/core/build.gd (Build.is_debug())
  </read_first>
  <behavior>
    - `Log.enabled(Category.GAMEPLAY, Level.DEBUG)` é `false` quando `Build.is_debug()` é `false` (nível DEBUG some em release), independente do que foi configurado por categoria.
    - `Log.enabled(cat, lvl)` respeita um nível mínimo configurável por categoria via `set_min_level(cat, lvl)`; abaixo do mínimo, `false`.
    - Uma chamada a `Log.debug(cat, key, data)` com o nível desabilitado **não invoca `write()` de nenhum sink** — testado com um `LogSink` de teste (spy) que conta chamadas.
    - `FileLogSink` gira o arquivo corrente para um novo arquivo quando o tamanho ultrapassa 2 MB, mantendo no máximo 5 arquivos; o arquivo mais antigo além do 5º é descartado.
    - `Log.Category.GAMEPLAY` (e as outras 9) resolvem para os mesmos valores que
      `LogCategory.Category.GAMEPLAY` — `Log` expõe `Category` como alias, não redeclara o enum.
  </behavior>
  <action>
    1. `apps/mobile/src/core/log/log_category.gd`:
    ```gdscript
    class_name LogCategory
    extends RefCounted

    enum Category {
        GAMEPLAY,
        TERRITORY,
        AI,
        INPUT,
        UI,
        SAVE,
        NETWORK,
        AUDIO,
        PERFORMANCE,
        ERROR,
    }
    ```

    2. `apps/mobile/src/core/log/log_sink.gd` — interface abstrata:
    ```gdscript
    class_name LogSink
    extends RefCounted

    func write(category: int, level: int, key: String, data: Dictionary) -> void:
        push_error("LogSink.write() não implementado em %s" % [get_script()])
    ```

    3. `apps/mobile/src/core/log/file_log_sink.gd`:
    ```gdscript
    class_name FileLogSink
    extends LogSink

    const MAX_BYTES: int = 2 * 1024 * 1024
    const MAX_FILES: int = 5
    const LOG_DIR: String = "user://logs/"
    const BASE_NAME: String = "volta"

    var _current: FileAccess = null
    var _current_size: int = 0

    func write(category: int, level: int, key: String, data: Dictionary) -> void:
        _ensure_open()
        var line := "%s [%d:%d] %s %s\n" % [Time.get_datetime_string_from_system(true), category, level, key, JSON.stringify(data)]
        var bytes := line.to_utf8_buffer()
        if _current_size + bytes.size() > MAX_BYTES:
            _rotate()
        _current.store_buffer(bytes)
        _current.flush()
        _current_size += bytes.size()

    func _ensure_open() -> void:
        if _current != null:
            return
        DirAccess.make_dir_recursive_absolute(LOG_DIR)
        var path := "%s%s_0.log" % [LOG_DIR, BASE_NAME]
        _current = FileAccess.open(path, FileAccess.READ_WRITE if FileAccess.file_exists(path) else FileAccess.WRITE)
        if _current == null:
            _current = FileAccess.open(path, FileAccess.WRITE)
        _current.seek_end()
        _current_size = _current.get_length()

    func _rotate() -> void:
        _current.close()
        var dir := DirAccess.open(LOG_DIR)
        var oldest := "%s%s_%d.log" % [LOG_DIR, BASE_NAME, MAX_FILES - 1]
        if FileAccess.file_exists(oldest):
            dir.remove(oldest)
        for i in range(MAX_FILES - 2, -1, -1):
            var src := "%s%s_%d.log" % [LOG_DIR, BASE_NAME, i]
            var dst := "%s%s_%d.log" % [LOG_DIR, BASE_NAME, i + 1]
            if FileAccess.file_exists(src):
                dir.rename(src, dst)
        _current = FileAccess.open("%s%s_0.log" % [LOG_DIR, BASE_NAME], FileAccess.WRITE)
        _current_size = 0
    ```
       (Ajuste chamadas de `FileAccess`/`DirAccess` conforme necessário para compilar limpo em
       Godot 4.3 — a intenção acima é o contrato: nome de arquivo por índice, rotação por
       shift, teto de 5 arquivos × 2 MB.)

    4. `apps/mobile/src/core/log/log.gd` — autoload `Log` (registrado no passo 6; sem
       `class_name`, ver `<autoload_rules>`):
    ```gdscript
    # Autoload "Log" (project.godot). Sem class_name: Godot 4.3 rejeita class_name igual ao
    # nome de um autoload ("hides an autoload singleton").
    extends Node

    const Category := LogCategory.Category
    enum Level { DEBUG, INFO, WARN, ERROR }

    var _min_level: Dictionary = {}
    var _sinks: Array[LogSink] = []

    func _ready() -> void:
        for cat in Category.values():
            _min_level[cat] = Level.INFO
        _sinks.append(FileLogSink.new())

    func set_min_level(category: int, level: int) -> void:
        _min_level[category] = level

    func enabled(category: int, level: int) -> bool:
        if level == Level.DEBUG and not Build.is_debug():
            return false
        return level >= _min_level.get(category, Level.INFO)

    func debug(category: int, key: String, data: Dictionary = {}) -> void:
        _log(category, Level.DEBUG, key, data)

    func info(category: int, key: String, data: Dictionary = {}) -> void:
        _log(category, Level.INFO, key, data)

    func warn(category: int, key: String, data: Dictionary = {}) -> void:
        _log(category, Level.WARN, key, data)

    func error(category: int, key: String, data: Dictionary = {}) -> void:
        _log(category, Level.ERROR, key, data)

    func _log(category: int, level: int, key: String, data: Dictionary) -> void:
        if not enabled(category, level):
            return
        if Build.is_debug():
            print("[%s] %s %s" % [Level.keys()[level], key, data])
        for sink in _sinks:
            sink.write(category, level, key, data)
    ```
       Adicione um setter de teste `func _set_sinks_for_test(sinks: Array[LogSink]) -> void:
       _sinks = sinks` — usado só pelo teste, para injetar um spy sem tocar disco.

    5. `apps/mobile/tests/unit/test_log.gd` (GUT, `extends GutTest`):
       - `test_disabled_level_does_not_call_sink`: cria um `LogSink` de teste local
         (`class TestSink extends LogSink` com contador `calls: int`), injeta via
         `_set_sinks_for_test`, chama `Log.debug(...)` com um mínimo configurado acima de
         DEBUG, assevera `sink.calls == 0`. (`Log` no teste é o singleton autoload.)
       - `test_level_respected_per_category`: `set_min_level(Category.AI, Level.WARN)`;
         assevera `enabled(Category.AI, Level.INFO) == false` e
         `enabled(Category.AI, Level.WARN) == true`; assevera que outra categoria não
         configurada continua em INFO.
       - `test_file_sink_rotates`: instancia `FileLogSink` isolado (caminho de teste, não o
         de produção, ou usa o mesmo `user://logs/` mas limpa antes/depois), escreve
         entradas suficientes para ultrapassar `MAX_BYTES` pelo menos uma vez, assevera que
         existe mais de um arquivo `volta_*.log` e que nenhum índice passa de `MAX_FILES - 1`.

    6. Registre o autoload em `apps/mobile/project.godot` — adicione a seção **antes** de
       `[display]`:
    ```ini
    [autoload]

    Log="*res://src/core/log/log.gd"
    ```
       (`Bootstrap=` entra na Task 2, logo abaixo desta linha.) Sem este passo os testes que
       referenciam `Log` não resolvem o identificador e `check-project.sh` falha.
  </action>
  <verify>
    <automated>./tools/ci/check-project.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/core/log/log.gd` contains `const Category := LogCategory.Category`
    - `apps/mobile/src/core/log/log.gd` contains `func enabled(category: int, level: int) -> bool`
    - `apps/mobile/src/core/log/file_log_sink.gd` contains `MAX_BYTES: int = 2 * 1024 * 1024`
    - `apps/mobile/src/core/log/file_log_sink.gd` contains `MAX_FILES: int = 5`
    - `apps/mobile/src/core/log/log.gd` does NOT contain `class_name`
    - `grep -c '^Log="\*res://src/core/log/log.gd"' apps/mobile/project.godot` retorna 1
    - `apps/mobile/tests/unit/test_log.gd` contains 3 `func test_` definitions matching the three scenarios above
    - `./tools/ci/check-project.sh` exits 0
  </acceptance_criteria>
  <done>Log autoload compila, respeita nível por categoria, suprime sink quando desabilitado, e o FileLogSink gira corretamente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ServiceRegistry + Bootstrap</name>
  <files>
    apps/mobile/src/core/service_registry.gd, apps/mobile/src/core/bootstrap.gd,
    apps/mobile/project.godot,
    apps/mobile/tests/unit/test_service_registry.gd, apps/mobile/tests/integration/test_bootstrap.gd
  </files>
  <read_first>
    - docs/architecture/overview.md §4 (injeção de dependência, exemplo de ServiceRegistry)
    - .planning/phases/01-repository-foundation/01-CONTEXT.md (regra: erro tipado e legível em resolve())
  </read_first>
  <behavior>
    - `register(name, instance)` seguido de `resolve(name)` retorna a mesma instância.
    - `resolve()` de um nome não registrado retorna `null` **e** deixa uma mensagem legível
      recuperável via `get_last_error()` no formato estável `"service_not_found:<nome>"`
      (testável sem depender de capturar `push_error`).
    - `register()` de um nome já registrado não sobrescreve silenciosamente: falha e mantém
      a instância original.
    - `Bootstrap.boot()` executa os passos configurados **na ordem declarada**, registra cada
      um no `ServiceRegistry`, e — se um passo **não essencial** falhar (fábrica retorna
      `null`) — continua para o próximo em vez de abortar. Se um passo **essencial** falhar,
      o boot para ali (não tenta os passos seguintes) e emite `boot_failed`.
  </behavior>
  <action>
    1. `apps/mobile/src/core/service_registry.gd`:
    ```gdscript
    class_name ServiceRegistry
    extends RefCounted

    var _services: Dictionary = {}
    var _last_error: String = ""

    func register(service_name: String, instance: Object) -> bool:
        if _services.has(service_name):
            _last_error = "service_already_registered:%s" % service_name
            push_error("ServiceRegistry: '%s' já registrado" % service_name)
            return false
        _services[service_name] = instance
        return true

    func resolve(service_name: String) -> Object:
        if not _services.has(service_name):
            _last_error = "service_not_found:%s" % service_name
            push_error("ServiceRegistry: serviço '%s' não encontrado. Registrados: %s" % [service_name, _services.keys()])
            return null
        return _services[service_name]

    func has(service_name: String) -> bool:
        return _services.has(service_name)

    func get_last_error() -> String:
        return _last_error
    ```

    2. `apps/mobile/src/core/bootstrap.gd` — autoload `Bootstrap` (o segundo e último; sem
       `class_name`, ver `<autoload_rules>`). Roda `boot()` sozinho ao entrar na árvore — o
       `_ready()` de um autoload executa antes da cena principal:
    ```gdscript
    # Autoload "Bootstrap" (project.godot). Sem class_name: Godot 4.3 rejeita class_name igual
    # ao nome de um autoload ("hides an autoload singleton").
    extends Node

    signal boot_completed(registry: ServiceRegistry)
    signal boot_failed(service_name: String, reason: String)

    ## Cena carregada ao fim do boot. Vazio nesta fase; o Plano 01-10 troca esta linha para
    ## "res://scenes/main.tscn" (edição de dado, sem lógica nova).
    var next_scene_path: String = ""

    var registry: ServiceRegistry = ServiceRegistry.new()
    var boot_order: Array[String] = []
    var _steps: Array[Dictionary] = []

    func _ready() -> void:
        configure_steps(_default_steps())
        boot()

    func configure_steps(steps: Array[Dictionary]) -> void:
        _steps = steps

    func boot() -> void:
        boot_order.clear()
        for step in _steps:
            var service_name: String = step["name"]
            var essential: bool = step.get("essential", true)
            var factory: Callable = step["factory"]
            var instance: Object = factory.call()
            if instance == null:
                var msg := "boot step '%s' retornou null" % service_name
                if Log:
                    Log.error(Log.Category.ERROR, "boot_step_failed", {"service": service_name, "essential": essential})
                boot_failed.emit(service_name, msg)
                if essential:
                    return
                continue
            registry.register(service_name, instance)
            boot_order.append(service_name)
            if Log:
                Log.info(Log.Category.GAMEPLAY, "boot_step_ok", {"service": service_name})
        boot_completed.emit(registry)
        _load_next_scene()

    func _default_steps() -> Array[Dictionary]:
        # Passos concretos de produção (Config, Save, EventBus) são adicionados pelos
        # planos que os implementam (01-04, 01-05, 01-06) chamando configure_steps() com
        # a lista completa antes de boot() — ou, em produção, sobrescrevendo este método.
        # Nesta fase, apenas Log (já vivo como autoload) é re-registrado para lookup
        # consistente via ServiceRegistry.
        return [
            {"name": "log", "essential": true, "factory": func() -> Object: return Log},
        ]

    func _load_next_scene() -> void:
        if next_scene_path == "" or not is_inside_tree():
            return  # testes instanciam fora da árvore; nada a carregar nesta fase
        get_tree().change_scene_to_file.call_deferred(next_scene_path)
    ```

    3. `apps/mobile/scenes/boot.tscn` já existe (Plano 01-01, nó `Boot` sem script) e continua
       sendo a cena principal placeholder — **não** anexe script nela; o boot acontece no
       autoload.

    4. Em `apps/mobile/project.godot`, complete a seção `[autoload]` criada na Task 1 para
       ficar **exatamente** assim (ordem importa — `Log` primeiro):
    ```ini
    [autoload]

    Log="*res://src/core/log/log.gd"
    Bootstrap="*res://src/core/bootstrap.gd"
    ```
       Nenhum outro autoload, agora ou em qualquer fase futura (A01-15).

    5. `apps/mobile/tests/unit/test_service_registry.gd`:
       - `test_register_then_resolve_returns_same_instance`
       - `test_resolve_missing_returns_null_with_readable_error`: assevera
         `registry.get_last_error() == "service_not_found:missing"`
       - `test_register_duplicate_keeps_original`: registra `"x"` duas vezes com instâncias
         diferentes, assevera que `resolve("x")` ainda é a primeira instância e
         `get_last_error() == "service_already_registered:x"`

    6. `apps/mobile/tests/integration/test_bootstrap.gd` (GUT integration). `Bootstrap` no
       teste é o singleton já instanciado — crie instâncias novas com
       `const BootstrapScript := preload("res://src/core/bootstrap.gd")` e
       `BootstrapScript.new()`, sem adicionar à árvore — chame `configure_steps()` e `boot()`
       diretamente, sem depender de `_ready()`):
       - `test_boot_order_is_deterministic`: configura 3 passos fake com fábricas que
         retornam objetos simples (`RefCounted.new()`), roda `boot()` duas vezes (em duas
         instâncias novas de `Bootstrap`), compara `boot_order` — devem ser idênticos.
       - `test_non_essential_failure_does_not_abort_boot`: configura 3 passos, o do meio
         `essential: false` com fábrica que retorna `null`; assevera que os passos 1 e 3
         terminam em `boot_order`, e que `boot_failed` foi emitido para o passo 2 (usar
         `watch_signals(bootstrap)` + `assert_signal_emitted`).
       - `test_essential_failure_stops_boot`: passo 1 `essential: true` retorna `null`;
         assevera `boot_order.is_empty()` e que os passos seguintes não rodaram (nenhum
         registrado no `registry`).
  </action>
  <verify>
    <automated>./tools/ci/check-project.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/core/service_registry.gd` contains `func get_last_error() -> String`
    - `apps/mobile/src/core/bootstrap.gd` contains `signal boot_completed(registry: ServiceRegistry)`
    - `apps/mobile/src/core/bootstrap.gd` contains `func configure_steps(steps: Array[Dictionary]) -> void`
    - `apps/mobile/src/core/bootstrap.gd` contains `var next_scene_path: String = ""`
    - `apps/mobile/src/core/bootstrap.gd` does NOT contain `class_name`
    - `grep -c 'run/main_scene="res://scenes/boot.tscn"' apps/mobile/project.godot` retorna 1
    - `grep -c '^Log="\*res://src/core/log/log.gd"' apps/mobile/project.godot` retorna 1
    - `grep -c '^Bootstrap="\*res://src/core/bootstrap.gd"' apps/mobile/project.godot` retorna 1
    - `grep -cE '^[A-Za-z_]+="\*?res://' apps/mobile/project.godot` retorna 2 (exatamente dois autoloads)
    - `apps/mobile/tests/integration/test_bootstrap.gd` contains `preload("res://src/core/bootstrap.gd")`
    - `apps/mobile/tests/unit/test_service_registry.gd` contains 3 `func test_` definitions
    - `apps/mobile/tests/integration/test_bootstrap.gd` contains 3 `func test_` definitions
    - `./tools/ci/check-project.sh` exits 0
  </acceptance_criteria>
  <done>ServiceRegistry e Bootstrap implementados; Log e Bootstrap são os dois únicos autoloads (nessa ordem, sem class_name); testes cobrem ordem determinística e falha essencial vs não-essencial.</done>
</task>

</tasks>

<verification>
`./tools/ci/check-project.sh` exits 0. `Log` e `Bootstrap` são os dois únicos autoloads
(`project.godot`), nessa ordem, sem `class_name`. `Bootstrap` roda `boot()` ao entrar na
árvore e é testável isoladamente com passos falsos, sem depender de nenhum outro serviço
concreto ainda não implementado.
</verification>

<success_criteria>
Nenhum `Global.gd` no projeto. Ordem de boot determinística e documentada. `Log` respeita
nível por categoria e não aloca quando desabilitado. Base pronta para Config (01-04), Save
(01-05) e EventBus (01-06) se registrarem no mesmo `ServiceRegistry`.
</success_criteria>

<output>
Após completar, crie `.planning/phases/01-repository-foundation/01-02-SUMMARY.md`.
</output>
