---
phase: 26.1-correcao-quality-gate
plan: 5
subsystem: ci
tags: [godot, gdscript, ci, quality-gate, regex, progression]

# Dependency graph
requires: []
provides:
  - "regra 4 do validate-repo.sh com limite de palavra a direita, sem falsos positivos e sem afrouxamento (prova negativa registrada)"
  - "regra 6 do validate-repo.sh com PLACEHOLDER-ART-004/Replacement na mesma linha"
  - "RemoteProfileRepository.load_profile() corrigido e coberto por teste"
affects: [26.1-06-verificacao-gate-verde]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Prova negativa: criar violacao real temporaria, confirmar reprovacao, remover — mesmo padrao de tests/tools/run_negative_checks.sh"
    - "Sintaxe isolada com godot --headless --script <path> --check-only para diagnosticar erro de compilacao sem rodar a suite inteira"
    - "git commit -m \"...\" -- <caminho> (pathspec direto no commit, nao so no add) para nao depender do indice git compartilhado entre executores paralelos"

key-files:
  created:
    - apps/mobile/tests/unit/test_remote_profile_repository.gd
  modified:
    - tools/ci/validate-repo.sh
    - apps/mobile/src/ui/design_system/theme_service.gd
    - apps/mobile/src/progression/remote_profile_repository.gd

key-decisions:
  - "UUID.v4() em remote_profile_repository.gd referenciava uma classe que nunca existiu no projeto e quebrava a compilacao do arquivo inteiro (Rule 3 - blocking issue); corrigido com gerador de UUID v4 local ao arquivo, sem criar classe/arquivo compartilhado novo, para nao expandir o escopo do plano"
  - "remote_leaderboard_repository.gd tem o mesmo bug UUID.v4() mas nao esta em files_modified de nenhum plano desta fase — deixado intacto e registrado em deferred-items.md para fase futura de religacao"

patterns-established:
  - "Regex de deteccao de TODO com limite de palavra bilateral: (^|[^A-Za-z])TODO([^A-Za-z]|$)"

requirements-completed: []

# Metrics
duration: 22min
completed: 2026-09-01
---

# Phase 26.1 Plan 5: Correções Pontuais Summary

**Regex da regra 4 do quality gate ganhou limite de palavra (para de casar "TODOS" em português e a string do próprio check_release_build.sh), placeholder ART-004 voltou para uma linha só, e o bug real `RemoteProfileRepository.load_profile()` foi corrigido — junto com um segundo bug de compilação (`UUID.v4()` inexistente) achado no caminho, coberto por teste.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-01T03:10:00Z
- **Completed:** 2026-09-01T03:32:39Z
- **Tasks:** 4 completadas (1 TDD com RED+GREEN)
- **Files modified:** 3 modificados, 1 criado (mais 1 `.uid` gerado pelo Godot)

## Accomplishments
- Regra 4 do `validate-repo.sh`: novo regex `(^|[^A-Za-z])TODO([^A-Za-z]|$)` para de casar `TODOS`/`TODOs` (português) e a string literal de `check_release_build.sh`, sem suprimir nenhum arquivo de código
- **Prova negativa executada e registrada:** um TODO real sem referência de tarefa (`_tmp_negative_todo.gd`) continuou sendo reprovado pela regra 4 após o ajuste — `PROVA NEGATIVA OK`, arquivo temporário removido, `git status` limpo
- Regra 6: `PLACEHOLDER-ART-004` e `Replacement: GSD 08` reescritos na mesma linha em `theme_service.gd:87`, no mesmo formato de `dev_overlay.gd:10`
- `RemoteProfileRepository.load_profile()` corrigido para chamar `local_cache.get_profile()` (o método real), com `api.get_data()` protegido contra `api == null`
- Bug adicional descoberto e corrigido: `UUID.v4()` em `save_profile()` referenciava uma classe inexistente e quebrava a compilação do arquivo inteiro — confirmado com `godot --check-only` antes e depois da correção
- `./tools/ci/validate-repo.sh` sai com código 0 e todas as 10 regras OK (confirmado no fim da execução, com os planos 01/02/03 desta fase também já concluídos)
- `./tools/ci/test-client.sh` sai com código 0 (50/50 testes GUT passando)

## Task Commits

Each task was committed atomically:

1. **Task 1: Limite de palavra na regra 4** - `efcb19a` (fix) — *ver nota de desvio abaixo: este commit também incluiu, por corrida de índice git compartilhado entre executores paralelos, mudanças de outros dois planos (não alteradas por mim)*
2. **Task 2: Prova negativa da regra 4** - sem commit (task de verificação pura; arquivo temporário criado e removido, nada ficou no working tree)
3. **Task 3: Placeholder ART-004 na mesma linha** - `db59d11` (fix), isolado
4. **Task 4: Corrigir RemoteProfileRepository (TDD)**:
   - RED: `097e009` (test) — teste falho, isolado
   - GREEN: `ff7e7a8` (fix) — inclui também a correção do bug `UUID.v4()`, isolado
   - `.uid` do teste: `4de1d30` (chore), isolado

**Plan metadata:** (a ser criado no commit final de documentação)

_Nota: a partir da Task 3, todo commit usou `git commit -m "..." -- <caminho>` (pathspec direto no `commit`, não só no `add`) para blindar contra o índice git compartilhado entre os até 5 executores paralelos desta fase — ver deviations abaixo._

## Files Created/Modified
- `tools/ci/validate-repo.sh` - regex da regra 4 com limite de palavra à direita + `check_release_build.sh` na lista de excluídos
- `apps/mobile/src/ui/design_system/theme_service.gd` - comentário do placeholder reescrito em uma linha
- `apps/mobile/src/progression/remote_profile_repository.gd` - `load_profile()` delega a `get_profile()`, `api.get_data()` protegido contra null, `UUID.v4()` substituído por gerador local
- `apps/mobile/tests/unit/test_remote_profile_repository.gd` - teste cobrindo o bug da auditoria da Fase 26

## Decisions Made
- **Bug de compilação não previsto pelo plano:** `UUID.v4()` (linha 21 de `remote_profile_repository.gd`, dentro de `save_profile()`) referenciava uma classe que nunca existiu no projeto (confirmado com `grep -rn UUID` no projeto inteiro e com `godot --headless --script ... --check-only`, que reproduz `Identifier "UUID" not declared in the current scope`). Isso quebrava a compilação do arquivo inteiro, bloqueando totalmente a Task 4 mesmo com a correção da linha 16 aplicada (Rule 3 — blocking issue). Corrigi com um gerador de UUID v4 (RFC 4122) local ao próprio arquivo, sem criar uma classe/arquivo novo compartilhado — respeitando a instrução do plano de não mexer "no resto do arquivo" além do estritamente necessário para destravar a compilação e o teste.
- **`remote_leaderboard_repository.gd:22` tem o mesmo bug `UUID.v4()`**, confirmado com o mesmo `--check-only`, mas esse arquivo não está em `files_modified` de nenhum plano desta fase — deixado intacto, registrado em `deferred-items.md` para uma fase futura de religação de `progression/`.
- QLT-06 (requisito do frontmatter dos planos 04/05) **não foi marcado completo** em `REQUIREMENTS.md`: o texto real de QLT-06 lá ("Lançamento com rollout gradual e monitoramento") não corresponde ao uso que os planos desta fase fazem do código (fechamento do quality gate) — mesma discrepância já registrada pelo plano 26-01 em STATE.md. Meu papel é fechar minhas duas tasks, não decidir sozinho a reinterpretação de um requisito de outra fase.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `UUID.v4()` inexistente quebrava a compilação de `remote_profile_repository.gd`**
- **Found during:** Task 4 (correção de `load_profile()`)
- **Issue:** Após corrigir a linha 16, o teste continuava falhando com o mesmo erro genérico de compilação (`Nonexistent function 'new' in base 'GDScript'`). Isolei a causa com `godot --headless --script res://src/progression/remote_profile_repository.gd --check-only`, que apontou `Identifier "UUID" not declared in the current scope` na linha 21 (`save_profile()`). Busca em todo o projeto (`grep -rni uuid`) confirmou que `UUID` nunca existiu como classe nem autoload — bug pré-existente, não causado por mim.
- **Fix:** Substituí `UUID.v4()` por uma função privada `_generate_idempotency_key()` (RFC 4122 v4) definida no próprio arquivo, sem tocar em `ApiClient`/`OfflineQueue` nem criar arquivo/classe nova.
- **Files modified:** `apps/mobile/src/progression/remote_profile_repository.gd`
- **Verification:** `godot --check-only` limpo depois da correção; teste isolado 1/1 passou; suíte completa `./tools/ci/test-client.sh` saiu com código 0 (50/50)
- **Committed in:** `ff7e7a8` (parte do commit GREEN da Task 4)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessário para que a Task 4 fosse sequer completável — sem isso o arquivo nunca compilava, teste nenhum. Sem scope creep: nenhuma classe nova criada, `ApiClient`/`OfflineQueue` não tocados, e o bug irmão em `remote_leaderboard_repository.gd` foi deliberadamente deixado fora (não é meu arquivo).

## Issues Encountered

**Corrida de índice git compartilhado entre até 5 executores paralelos da fase 26.1.** Ao final da Task 1, `git add tools/ci/validate-repo.sh` seguido de `git commit -m "..."` **sem pathspec** commitou o índice inteiro, que já continha `apps/mobile/src/gameplay/match_director.gd` (plano 01) e `apps/mobile/tests/unit/test_seal_solver.gd` (plano 02) staged por processos concorrentes. Resultado: o commit `efcb19a` empacotou os três. Conferido byte a byte: o diff de `tools/ci/validate-repo.sh` dentro desse commit é exatamente o pretendido pela Task 1 — nenhum conteúdo meu vazou para os arquivos alheios nem o contrário. Não reescrevi o histórico porque `efcb19a` já tinha um commit de outro plano em cima (`df97838`); um `reset`/rebase teria descartado trabalho alheio sem necessidade, já que não houve perda de dado, só atribuição cruzada de commit. Documentado em `deferred-items.md` (confirmação cruzada com os relatos dos planos 01 e 02, que descrevem o mesmo incidente do lado deles) e em `STATE.md`. A partir da Task 3, todo commit usou `git commit -m "..." -- <caminho>` (pathspec direto no `commit`, não só no `add`), que ignora o resto do índice compartilhado — confirmado isolado com `git show --stat` em cada commit seguinte (`db59d11`, `097e009`, `ff7e7a8`, `4de1d30`).

Também houve uma colisão pontual de lock do índice (`Unable to create '.git/index.lock'`) durante o commit GREEN da Task 4, resolvida esperando o lock liberar e repetindo o comando (nenhum dado perdido).

## User Setup Required

None - nenhuma configuração de serviço externo envolvida.

## Next Phase Readiness
- `./tools/ci/validate-repo.sh` sai com código 0 e as 10 regras OK, confirmando que o plano 06 (verificação do gate verde) já tem o resultado esperado do lado deste plano e dos planos 01/02/03 (também concluídos no momento desta verificação).
- `remote_leaderboard_repository.gd:22` continua com o bug `UUID.v4()` — não bloqueia esta fase (fora do escopo declarado), mas deveria ser corrigido antes de qualquer fase que religue `progression/leaderboard/` de verdade.

---
*Phase: 26.1-correcao-quality-gate*
*Completed: 2026-09-01*

## Self-Check: PASSED

- FOUND: tools/ci/validate-repo.sh
- FOUND: apps/mobile/src/ui/design_system/theme_service.gd
- FOUND: apps/mobile/src/progression/remote_profile_repository.gd
- FOUND: apps/mobile/tests/unit/test_remote_profile_repository.gd
- FOUND: efcb19a, db59d11, 097e009, ff7e7a8, 4de1d30 (todos os commits de tarefa)
